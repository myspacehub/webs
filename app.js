const STORAGE_KEY = "fujian-highschool-h5-progress-v1";

const COURSE = [
  {
    name: "语文",
    volumes: [
      {
        name: "必修上册",
        units: [
          "第一单元 现代文阅读（文学类文本）",
          "第二单元 实用性阅读与交流",
          "第三单元 文学阅读与写作（古代诗文）",
          "第四单元 思辨性阅读与表达",
          "第五单元 整本书阅读《乡土中国》",
          "第六单元 文学阅读与写作",
          "第七单元 实用性阅读与交流",
          "第八单元 中国革命传统作品研习",
        ],
      },
      {
        name: "必修下册",
        units: [
          "第一单元 古代诗文阅读",
          "第二单元 现代文阅读",
          "第三单元 思辨性阅读与表达",
          "第四单元 古代诗文阅读",
          "第五单元 整本书阅读《红楼梦》",
          "第六单元 实用性阅读与交流",
          "第七单元 科学与文化论著研习",
        ],
      },
      {
        name: "选择性必修上册",
        units: [
          "第一单元 中国革命传统作品研习",
          "第二单元 古代诗文阅读",
          "第三单元 思辨性阅读与表达",
          "第四单元 科学与文化论著研习",
          "第五单元 写作单元",
        ],
      },
      {
        name: "选择性必修中册",
        units: [
          "第一单元 中国革命传统作品研习",
          "第二单元 古代诗文阅读",
          "第三单元 思辨性阅读与表达",
          "第四单元 外国作家作品研习",
          "第五单元 写作单元",
        ],
      },
      {
        name: "选择性必修下册",
        units: [
          "第一单元 中华传统文化经典研习",
          "第二单元 古代诗文阅读",
          "第三单元 现当代作家作品研习",
          "第四单元 外国作家作品研习",
          "第五单元 写作单元",
        ],
      },
    ],
  },
  {
    name: "数学（人教A版）",
    volumes: [
      {
        name: "必修第一册",
        units: [
          "第一章 集合与函数概念",
          "第二章 基本初等函数（Ⅰ）",
          "第三章 函数的应用",
          "第四章 指数函数与对数函数",
          "第五章 三角函数",
        ],
      },
      {
        name: "必修第二册",
        units: [
          "第六章 平面向量及其应用",
          "第七章 复数",
          "第八章 立体几何初步",
          "第九章 统计",
          "第十章 概率",
        ],
      },
      {
        name: "选择性必修第一册",
        units: [
          "第一章 空间向量与立体几何",
          "第二章 直线和圆的方程",
          "第三章 圆锥曲线的方程",
        ],
      },
      {
        name: "选择性必修第二册",
        units: [
          "第四章 数列",
          "第五章 一元函数的导数及其应用",
          "第六章 计数原理",
        ],
      },
      {
        name: "选择性必修第三册",
        units: [
          "第七章 随机变量及其分布",
          "第八章 成对数据的统计分析",
        ],
      },
    ],
  },
  {
    name: "英语",
    volumes: [
      {
        name: "必修第一册",
        units: [
          "Unit 1 Friendship",
          "Unit 2 Travelling Around",
          "Unit 3 Sports and Fitness",
          "Unit 4 Natural Disasters",
          "Unit 5 Languages Around the World",
        ],
      },
      {
        name: "必修第二册",
        units: [
          "Unit 1 Cultural Heritage",
          "Unit 2 Wildlife Protection",
          "Unit 3 The Internet",
          "Unit 4 History and Traditions",
          "Unit 5 Music",
        ],
      },
      {
        name: "必修第三册",
        units: [
          "Unit 1 Festivals and Celebrations",
          "Unit 2 Morals and Virtues",
          "Unit 3 Diverse Cultures",
          "Unit 4 Space Exploration",
          "Unit 5 The Value of Money",
        ],
      },
      {
        name: "选择性必修第一册",
        units: [
          "Unit 1 People of Achievement",
          "Unit 2 Looking into the Future",
          "Unit 3 Fascinating Parks",
          "Unit 4 Body Language",
          "Unit 5 Working the Land",
        ],
      },
      {
        name: "选择性必修第二册",
        units: [
          "Unit 1 Science and Scientists",
          "Unit 2 Bridging Cultures",
          "Unit 3 The Internet of Things",
          "Unit 4 Journey Across a Vast Land",
          "Unit 5 First Aid",
        ],
      },
      {
        name: "选择性必修第三册",
        units: [
          "Unit 1 Art",
          "Unit 2 Healthy Lifestyle",
          "Unit 3 Environmental Protection",
          "Unit 4 Adversity and Courage",
          "Unit 5 Poems",
        ],
      },
      {
        name: "选择性必修第四册",
        units: [
          "Unit 1 Science Fiction",
          "Unit 2 Iconic Attractions",
          "Unit 3 Sea Exploration",
          "Unit 4 Sharing",
          "Unit 5 Launching Your Career",
        ],
      },
    ],
  },
  {
    name: "物理",
    volumes: [
      {
        name: "必修第一册",
        units: [
          "第一章 运动的描述",
          "第二章 匀变速直线运动的研究",
          "第三章 相互作用——力",
          "第四章 运动和力的关系",
          "第五章 抛体运动",
        ],
      },
      {
        name: "必修第二册",
        units: [
          "第六章 圆周运动",
          "第七章 万有引力与宇宙航行",
          "第八章 机械能守恒定律",
        ],
      },
      {
        name: "必修第三册",
        units: [
          "第九章 静电场及其应用",
          "第十章 静电场中的能量",
          "第十一章 电路及其应用",
          "第十二章 电能 能量守恒定律",
          "第十三章 电磁感应与电磁波初步",
        ],
      },
      {
        name: "选择性必修第一册",
        units: [
          "第一章 动量守恒定律",
          "第二章 机械振动",
          "第三章 机械波",
          "第四章 光",
        ],
      },
      {
        name: "选择性必修第二册",
        units: [
          "第五章 交变电流",
          "第六章 电磁感应",
          "第七章 分子动理论",
          "第八章 热力学定律",
        ],
      },
      {
        name: "选择性必修第三册",
        units: [
          "第九章 固体、液体和物态变化",
          "第十章 磁场",
          "第十一章 电磁感应",
          "第十二章 原子结构与原子核",
        ],
      },
    ],
  },
  {
    name: "化学",
    volumes: [
      {
        name: "必修第一册",
        units: [
          "第一章 从实验学化学",
          "第二章 化学物质及其变化",
          "第三章 金属及其化合物",
          "第四章 非金属及其化合物",
        ],
      },
      {
        name: "必修第二册",
        units: [
          "第五章 物质结构 元素周期律",
          "第六章 化学反应与能量",
          "第七章 有机化合物",
          "第八章 化学与自然资源的开发利用",
        ],
      },
      {
        name: "选择性必修第一册",
        units: [
          "第一章 化学反应的热效应",
          "第二章 化学反应速率与化学平衡",
          "第三章 水溶液中的离子反应与平衡",
          "第四章 氧化还原反应与电化学",
        ],
      },
      {
        name: "选择性必修第二册",
        units: [
          "第一章 原子结构与性质",
          "第二章 分子结构与性质",
          "第三章 晶体结构与性质",
        ],
      },
      {
        name: "选择性必修第三册",
        units: [
          "第一章 有机化合物的结构特点与研究方法",
          "第二章 烃",
          "第三章 烃的衍生物",
          "第四章 生物大分子",
          "第五章 合成高分子",
        ],
      },
    ],
  },
  {
    name: "生物",
    volumes: [
      {
        name: "必修1 分子与细胞",
        units: [
          "第1章 走近细胞",
          "第2章 组成细胞的分子",
          "第3章 细胞的基本结构",
          "第4章 细胞的物质输入和输出",
          "第5章 细胞的能量供应和利用",
          "第6章 细胞的生命历程",
        ],
      },
      {
        name: "必修2 遗传与进化",
        units: [
          "第1章 遗传因子的发现",
          "第2章 基因和染色体的关系",
          "第3章 基因的本质",
          "第4章 基因的表达",
          "第5章 基因突变及其他变异",
          "第6章 生物的进化",
          "第7章 现代生物进化理论",
        ],
      },
      {
        name: "必修3 稳态与环境",
        units: [
          "第1章 人体的内环境与稳态",
          "第2章 动物和人体生命活动的调节",
          "第3章 植物的激素调节",
          "第4章 种群和群落",
          "第5章 生态系统及其稳定性",
          "第6章 生态环境的保护",
        ],
      },
      {
        name: "选择性必修1 稳态与调节",
        units: [
          "第1章 人体的内环境与稳态",
          "第2章 神经调节",
          "第3章 体液调节",
          "第4章 免疫调节",
          "第5章 植物生命活动的调节",
          "第6章 种群及其动态",
        ],
      },
      {
        name: "选择性必修2 生物与环境",
        units: [
          "第1章 种群及其动态",
          "第2章 群落及其演替",
          "第3章 生态系统及其稳定性",
          "第4章 生态环境的保护",
        ],
      },
      {
        name: "选择性必修3 生物技术与工程",
        units: [
          "第1章 发酵工程",
          "第2章 细胞工程",
          "第3章 基因工程",
          "第4章 生物技术的安全性与伦理问题",
        ],
      },
    ],
  },
  {
    name: "历史",
    volumes: [
      {
        name: "必修 中外历史纲要（上）",
        units: [
          "第一单元 古代中国的政治制度",
          "第二单元 古代希腊罗马的政治制度",
          "第三单元 近代西方资本主义政治制度的确立与发展",
          "第四单元 近代中国反侵略、求民主的潮流",
          "第五单元 从科学社会主义理论到社会主义制度的建立",
          "第六单元 现代中国的政治建设与祖国统一",
          "第七单元 现代中国的对外关系",
        ],
      },
      {
        name: "必修 中外历史纲要（下）",
        units: [
          "第一单元 古代文明的产生与发展",
          "第二单元 中古时期的世界",
          "第三单元 走向整体的世界",
          "第四单元 资本主义制度的确立",
          "第五单元 工业革命与马克思主义的诞生",
          "第六单元 世界殖民体系与亚非拉民族独立运动",
          "第七单元 两次世界大战、十月革命与国际秩序的演变",
          "第八单元 20世纪下半叶世界的新变化",
        ],
      },
      {
        name: "选择性必修1 国家制度与社会治理",
        units: [
          "第一单元 政治制度",
          "第二单元 官员的选拔与管理",
          "第三单元 法律与教化",
          "第四单元 民族关系与国家关系",
          "第五单元 基层治理与社会保障",
          "第六单元 货币与赋税制度",
        ],
      },
      {
        name: "选择性必修2 经济与社会生活",
        units: [
          "第一单元 食物生产与社会生活",
          "第二单元 生产工具与劳作方式",
          "第三单元 商业贸易与日常生活",
          "第四单元 村落、城镇与居住环境",
          "第五单元 交通与社会变迁",
          "第六单元 医疗与公共卫生",
        ],
      },
      {
        name: "选择性必修3 文化交流与传播",
        units: [
          "第一单元 源远流长的中华文化",
          "第二单元 世界文化的多元传统",
          "第三单元 人口迁徙与文化认同",
          "第四单元 商路、贸易与文化交流",
          "第五单元 战争与文化交锋",
          "第六单元 文化的传承与保护",
        ],
      },
    ],
  },
  {
    name: "地理",
    volumes: [
      {
        name: "必修第一册",
        units: [
          "第一章 宇宙中的地球",
          "第二章 地球上的大气",
          "第三章 地球上的水",
          "第四章 地表形态的塑造",
          "第五章 植被与土壤",
        ],
      },
      {
        name: "必修第二册",
        units: [
          "第一章 人口",
          "第二章 乡村与城镇",
          "第三章 产业区位因素",
          "第四章 交通运输布局与区域发展",
          "第五章 环境与发展",
        ],
      },
      {
        name: "选择性必修1 自然地理基础",
        units: [
          "第一章 地球的运动",
          "第二章 地表形态的塑造",
          "第三章 大气的运动",
          "第四章 水的运动",
          "第五章 自然环境的整体性与差异性",
        ],
      },
      {
        name: "选择性必修2 区域发展",
        units: [
          "第一章 区域与区域发展",
          "第二章 区域发展",
          "第三章 区域协调发展",
          "第四章 区域生态环境建设",
        ],
      },
      {
        name: "选择性必修3 资源、环境与国家安全",
        units: [
          "第一章 自然资源与国家安全",
          "第二章 生态保护与国家安全",
          "第三章 环境治理与国家安全",
          "第四章 全球环境治理与国家安全",
        ],
      },
    ],
  },
  {
    name: "思想政治",
    volumes: [
      {
        name: "必修1 中国特色社会主义",
        units: [
          "第一课 社会主义从空想到科学、从理论到实践的发展",
          "第二课 只有社会主义才能救中国",
          "第三课 只有中国特色社会主义才能发展中国",
          "第四课 中国特色社会主义进入新时代",
        ],
      },
      {
        name: "必修2 经济与社会",
        units: [
          "第一课 生产资料所有制与经济体制",
          "第二课 我国的社会主义市场经济体制",
          "第三课 我国的经济发展",
          "第四课 我国的个人收入分配与社会保障",
        ],
      },
      {
        name: "必修3 政治与法治",
        units: [
          "第一课 中国共产党的领导",
          "第二课 人民当家作主",
          "第三课 全面依法治国",
          "第四课 民族区域自治制度和宗教工作基本方针",
        ],
      },
      {
        name: "必修4 哲学与文化",
        units: [
          "第一课 时代精神的精华",
          "第二课 探究世界的本质",
          "第三课 把握世界的规律",
          "第四课 认识社会与价值选择",
          "第五课 寻觅社会的真谛",
          "第六课 文化传承与文化创新",
        ],
      },
      {
        name: "选择性必修1 当代国际政治与经济",
        units: [
          "第一课 国家与国家的治理",
          "第二课 世界多极化",
          "第三课 经济全球化",
          "第四课 国际组织",
        ],
      },
      {
        name: "选择性必修2 法律与生活",
        units: [
          "第一课 民事权利与义务",
          "第二课 合同与侵权责任",
          "第三课 婚姻家庭",
          "第四课 劳动者与就业",
          "第五课 知识产权与诉讼",
        ],
      },
      {
        name: "选择性必修3 逻辑与思维",
        units: [
          "第一课 走进思维世界",
          "第二课 遵循逻辑思维规则",
          "第三课 运用辩证思维方法",
          "第四课 提高创新思维能力",
        ],
      },
    ],
  },
];

const SUBJECT_GUIDES = {
  语文: {
    aliases: ["语文"],
    objectives: [
      "把握「{unit}」的文体特征、主题脉络、表达方式与价值立意，形成从文本到观点的完整理解链。",
      "建立“作者—时代—文本—读者”四维阅读框架，能解释作品为什么这样写、这样写产生什么效果。",
      "积累关键词、典型句式、论证材料和审美表达，把阅读成果迁移到作文、微写作和口语表达中。",
      "训练概括、赏析、比较、探究四类核心能力，形成可复用的答题路径。",
    ],
    core: [
      "文本层：梳理标题、结构、线索、段落关系、叙述视角、意象或核心概念。",
      "语言层：分析关键词、长短句、修辞、语气、节奏、引用与典故的表达效果。",
      "思想层：提炼主旨、价值判断、人物态度、文化内涵与现实意义。",
      "任务层：区分信息筛选、内容概括、句段赏析、论证分析、开放探究等题型。",
      "迁移层：把单元阅读方法转化为写作提纲、素材卡片和表达模板。",
    ],
    methods: [
      "三遍阅读法：首读抓对象与结构，细读圈画矛盾与关键词，复读归纳主旨和手法。",
      "题干拆解法：标出限定词、对象词、能力词，先判断题型再组织答案。",
      "证据闭环：观点必须回到文本语句，做到“判断—依据—分析—效果”。",
      "比较阅读：从对象、角度、手法、情感、价值五个维度建立异同表。",
      "写作转化：每个单元至少沉淀一个观点、三个素材、两种表达技法。",
    ],
    exam: [
      "文学类文本重点练环境、人物、情节、叙述、语言与主题的综合赏析。",
      "实用类文本重点练信息筛选、图文转换、材料整合和任务情境表达。",
      "古诗文重点练实词虚词、句式翻译、形象意境、情感态度与文化常识。",
      "论述类文本重点练论点论据论证、概念关系、逻辑漏洞和观点迁移。",
      "作文重点练审题立意、结构推进、材料扣合、语言张力和思辨深度。",
    ],
    errors: [
      "只摘抄不分析，导致答案有材料无结论。",
      "把手法名称当答案，缺少“为什么有效”的表达效果。",
      "开放题脱离文本，观点漂亮但证据不足。",
      "作文只有态度没有层次，缺少概念界定和递进论证。",
    ],
    keywords: [
      {
        match: ["现代文", "文学类", "文学阅读"],
        title: "文学类文本精读",
        points: [
          "小说：抓人物愿望、冲突推进、情节转折、叙述视角与结尾意味。",
          "散文：抓线索、意象、情感波澜、虚实结合和语言风格。",
          "诗歌化表达：关注留白、象征、反复、节奏和感官描写。",
          "主题探究：从个体命运、时代背景、人性观察和文化价值四层推进。",
          "答题模板：手法名称 + 文本依据 + 情感/人物/主题效果。",
        ],
      },
      {
        match: ["实用性", "交流", "论著"],
        title: "实用类文本与论著阅读",
        points: [
          "识别文本功能：说明、新闻、访谈、科普、报告、评论的表达目的不同。",
          "筛选信息时区分事实、观点、数据、例证、推断和作者态度。",
          "多材料整合要先定任务，再按维度合并同类信息。",
          "图表题抓标题、单位、趋势、极值、变化原因和结论边界。",
          "表达任务强调对象意识、场景意识、语体意识和可执行建议。",
        ],
      },
      {
        match: ["古代诗文", "古代", "诗文", "传统文化"],
        title: "古诗文与传统文化",
        points: [
          "文言文：落实实词、虚词、通假、词类活用、特殊句式和断句逻辑。",
          "古诗词：抓意象群、声色动静、虚实关系、抒情方式和情感转折。",
          "文化常识：官职、礼制、科举、地理、纪年与典故要放回语境理解。",
          "翻译原则：字字落实、补出省略、调顺语序、保留专名。",
          "赏析路径：意象—画面—情感—手法—时代文化意蕴。",
        ],
      },
      {
        match: ["思辨", "表达", "写作"],
        title: "思辨表达与写作",
        points: [
          "概念界定：先说清讨论对象，避免偷换概念和泛泛而谈。",
          "论证结构：提出问题—分析原因—辨析关系—提出方案—回扣价值。",
          "常用关系：传统与现代、个人与社会、效率与公平、技术与人文。",
          "材料使用：名言、事实、现象和文本材料要服务于同一个中心论点。",
          "语言升级：用因果、转折、递进、让步句式增强思维层次。",
        ],
      },
      {
        match: ["乡土中国", "红楼梦", "整本书"],
        title: "整本书阅读",
        points: [
          "建立人物关系图、章节线索图、核心概念表和主题问题清单。",
          "《乡土中国》重点抓差序格局、礼治秩序、乡土社会、文字下乡等概念。",
          "《红楼梦》重点抓家族结构、人物命运、诗词判词、环境象征和悲剧意识。",
          "整本书笔记要包含摘录、旁批、章节概括、问题追问和主题短评。",
          "考试迁移常落在人物评价、概念阐释、情境应用和跨文本比较。",
        ],
      },
      {
        match: ["革命", "中国革命"],
        title: "革命传统作品研习",
        points: [
          "梳理历史背景、革命主体、叙事对象、精神品质和时代任务。",
          "分析宏大叙事与个人经验之间的连接方式。",
          "关注信仰、牺牲、奋斗、人民立场等价值关键词。",
          "避免空泛拔高，所有精神判断都要落实到人物行动和文本细节。",
          "写作迁移可形成家国情怀、青年责任、历史记忆类素材库。",
        ],
      },
    ],
  },
  数学: {
    aliases: ["数学"],
    objectives: [
      "明确「{unit}」的定义体系、符号语言、图形表征和运算规则。",
      "把概念、公式、定理、模型、题型串成一张可迁移的解题网络。",
      "训练从文字条件到数学表达、从图像直观到代数推导的双向转换。",
      "沉淀典型题的一题多解、一题多变和易错边界。",
    ],
    core: [
      "概念定义：关注适用条件、等价表述、反例和与旧知识的联系。",
      "公式定理：掌握来源、证明思路、使用前提和常见变形。",
      "图形图像：把图像特征、几何关系、数量关系互相转化。",
      "运算推理：保证符号规范、步骤闭合、分类完整、结果检验。",
      "模型意识：从函数、方程、不等式、向量、概率统计中选择合适工具。",
    ],
    methods: [
      "审题三步：圈条件、定目标、找桥梁，先画结构图再计算。",
      "分类讨论：按参数范围、符号正负、几何位置、事件互斥等标准拆分。",
      "数形结合：用图像判断趋势、范围、交点、对称和最值。",
      "特殊到一般：用特殊值试探，再推广为代数证明。",
      "检验意识：代回原式、检查定义域、端点、重复计数和单位。",
    ],
    exam: [
      "选择填空重速度：优先排除、特殊值、图像、估算和代入。",
      "解答题重过程：定义域、设参、化简、证明、结论都要完整。",
      "函数与导数常考单调性、零点、最值、恒成立和参数范围。",
      "解析几何常考轨迹、弦长、面积、斜率、定点定值和最值。",
      "概率统计常考样本、分布、期望方差、独立性和回归解释。",
    ],
    errors: [
      "忽视定义域、取值范围或参数限制。",
      "分类讨论漏情况，或讨论后没有合并结论。",
      "把必要条件当充分条件，推理方向反了。",
      "计算正确但数学表达不规范，导致过程分丢失。",
    ],
    keywords: [
      {
        match: ["集合"],
        title: "集合与逻辑基础",
        points: [
          "元素、集合、子集、真子集、空集、全集的符号与关系。",
          "交、并、补运算要配合数轴、Venn 图和不等式区间。",
          "命题条件中注意“且/或/非”的逻辑转换。",
          "常见易错：空集是任意集合子集；端点开闭决定区间关系。",
        ],
      },
      {
        match: ["函数", "指数", "对数", "三角"],
        title: "函数主线",
        points: [
          "函数三要素：定义域、对应关系、值域，三者缺一不可。",
          "单调性、奇偶性、周期性、最值、零点和图像变换是核心工具。",
          "指数对数关注底数范围、互逆关系、运算法则和单调性差异。",
          "三角函数关注弧度制、诱导公式、图像周期、最值和恒等变换。",
          "综合题常把函数与不等式、方程、导数、参数结合。",
        ],
      },
      {
        match: ["向量", "立体几何", "空间"],
        title: "向量与空间几何",
        points: [
          "平面向量关注线性运算、数量积、夹角、投影和坐标表示。",
          "空间向量把线面角、二面角、距离问题转化为坐标与法向量。",
          "立体几何先画图、标注垂直平行关系，再选择综合法或向量法。",
          "证明题重逻辑链：已知关系—判定定理—性质定理—结论。",
          "计算题易错在法向量方向、夹角补角和长度单位。",
        ],
      },
      {
        match: ["圆锥曲线", "直线", "圆"],
        title: "解析几何",
        points: [
          "直线方程要熟练切换点斜式、斜截式、一般式和参数式。",
          "圆的问题围绕圆心、半径、弦长、切线和点圆位置关系展开。",
          "椭圆、双曲线、抛物线重点掌握定义、标准方程、焦点、离心率。",
          "联立消元后常用韦达定理处理弦中点、面积、斜率和范围。",
          "警惕斜率不存在、判别式条件和参数导致的退化情况。",
        ],
      },
      {
        match: ["数列", "导数", "计数", "概率", "随机变量", "统计"],
        title: "高阶模型与统计",
        points: [
          "数列抓通项、递推、求和、单调和放缩证明。",
          "导数抓几何意义、单调区间、极值、最值、切线和恒成立。",
          "计数原理区分分类加法、分步乘法、排列、组合和分配问题。",
          "随机变量掌握分布列、期望、方差、二项分布和超几何模型。",
          "统计分析重在解释样本、相关、回归、残差和实际意义。",
        ],
      },
    ],
  },
  英语: {
    aliases: ["英语"],
    objectives: [
      "围绕「{unit}」建立主题语境、核心词汇、语法功能、阅读策略和输出任务。",
      "把单元话题转化为听说读写四项能力训练，而不是只背单词。",
      "积累高频表达、主题句、衔接词和写作语块，形成可迁移素材库。",
      "训练从语篇主旨到细节推断、从语言输入到真实表达的闭环。",
    ],
    core: [
      "Vocabulary：主题词、派生词、短语搭配、熟词生义和语境用法。",
      "Reading：主旨、结构、细节、推断、态度、指代和篇章逻辑。",
      "Grammar：时态语态、非谓语、定语从句、名词性从句、状语从句等功能表达。",
      "Writing：审题、体裁、段落结构、过渡衔接、亮点句和修改清单。",
      "Culture：比较中外文化，形成礼貌、得体、真实的跨文化表达。",
    ],
    methods: [
      "词汇四步：音形义搭配 + 例句 + 主题分类 + 输出造句。",
      "阅读三层：topic sentence 抓主旨，signal words 抓结构，details 支撑判断。",
      "语法入文：不要孤立刷题，要看语法如何服务叙述、说明、论证和描写。",
      "写作模板：开头点题、主体分层、例证支撑、结尾升华。",
      "复述训练：用 5W1H、时间线、因果链或问题解决链复述文本。",
    ],
    exam: [
      "阅读理解重定位、同义替换、段落功能和作者态度。",
      "七选五重上下文指代、衔接词、段首段中段尾功能。",
      "完形填空重语境、词义辨析、逻辑关系和情感色彩。",
      "语法填空重词性转换、谓语非谓语、从句连接词和固定搭配。",
      "应用文/读后续写重体裁格式、情节推进、动作描写和情感呼应。",
    ],
    errors: [
      "单词只背中文释义，不掌握搭配和语境。",
      "阅读题用常识代替文本证据。",
      "写作句子堆砌，缺少段落逻辑和衔接。",
      "语法填空忽略句子成分，凭感觉填词。",
    ],
    keywords: [
      {
        match: ["Friendship", "Morals", "Sharing", "Virtues", "Healthy"],
        title: "人与自我 / 人际关系",
        points: [
          "主题词：friendship, responsibility, empathy, respect, lifestyle, virtue。",
          "表达任务：建议、感谢、道歉、邀请、观点陈述和个人经历叙述。",
          "读写重点：人物品质、情感变化、冲突解决、价值判断。",
          "写作亮点：用具体动作和心理描写表现人物，而不是直接评价。",
        ],
      },
      {
        match: ["Travelling", "Cultural", "History", "Traditions", "Festivals", "Cultures", "Parks", "Attractions"],
        title: "文化与旅行主题",
        points: [
          "主题词：heritage, tradition, attraction, custom, diversity, preserve。",
          "阅读重点：地点介绍、历史沿革、文化价值、保护措施和游客体验。",
          "写作任务：景点推荐、文化介绍、节日活动报道、旅行计划。",
          "跨文化表达：既介绍事实，也解释背后的文化意义。",
        ],
      },
      {
        match: ["Internet", "Technology", "Science", "Scientists", "Future", "Space", "Fiction"],
        title: "科技与未来主题",
        points: [
          "主题词：innovation, exploration, artificial, digital, scientific, evidence。",
          "阅读重点：问题—技术方案—影响—争议—未来趋势。",
          "语法功能：被动语态、将来表达、非谓语和定语从句常用于科技说明。",
          "写作任务：科技利弊、未来生活、科学人物介绍、科普短文。",
        ],
      },
      {
        match: ["Sports", "Fitness", "First Aid", "Natural Disasters", "Environmental", "Wildlife"],
        title: "健康、安全与环境",
        points: [
          "主题词：fitness, rescue, protection, disaster, emergency, biodiversity。",
          "阅读重点：原因、危害、措施、流程、倡议和个人行动。",
          "表达任务：倡议信、急救说明、保护动物海报、灾害应对建议。",
          "语言特点：祈使句、情态动词、条件句和流程连接词。",
        ],
      },
      {
        match: ["Money", "Career", "Achievement", "Working the Land", "Music", "Art", "Poems"],
        title: "社会、职业与审美",
        points: [
          "主题词：career, achievement, value, creativity, agriculture, art, poem。",
          "阅读重点：人物成就、职业规划、价值冲突、艺术体验和社会贡献。",
          "输出任务：人物介绍、职业规划演讲、作品评论、观点短文。",
          "写作升级：用例证解释价值，用比较体现选择理由。",
        ],
      },
    ],
  },
  物理: {
    aliases: ["物理"],
    objectives: [
      "围绕「{unit}」建立物理情境、研究对象、受力/过程、规律方程和实验证据。",
      "训练把真实问题抽象成理想模型，再用图像、公式和守恒思想求解。",
      "掌握核心概念的适用条件，区分现象描述、定量规律和模型假设。",
      "形成“审题—建模—列式—求解—检验”的完整物理解题链。",
    ],
    core: [
      "对象与过程：明确研究对象、运动阶段、相互作用和边界条件。",
      "模型：质点、匀变速、圆周、弹簧振子、理想气体、点电荷等。",
      "规律：牛顿运动定律、能量守恒、动量守恒、电路规律、电磁感应规律。",
      "图像：x-t、v-t、F-t、U-I、E-x、B-t 图像的斜率、面积和截距含义。",
      "实验：控制变量、数据处理、误差分析和结论表达。",
    ],
    methods: [
      "先画图：运动图、受力图、能量图、电路图或场线图。",
      "分阶段：把复杂过程拆成若干可套用规律的阶段。",
      "选规律：动力学看力和加速度，能量看始末状态，动量看系统和相互作用。",
      "单位检验：所有结果必须检查量纲、方向、正负和合理范围。",
      "极限思维：用特殊情形判断公式和结果是否合理。",
    ],
    exam: [
      "选择题常考概念辨析、图像理解、极限判断和单位量纲。",
      "实验题常考仪器读数、步骤排序、数据拟合、误差来源。",
      "计算题常考多过程综合、连接体、临界条件和守恒定律组合。",
      "电磁题常考场、路、力、能的综合迁移。",
      "热学和原子物理常考概念、图像和规律适用条件。",
    ],
    errors: [
      "对象选错导致受力分析多力或漏力。",
      "把速度、加速度、位移方向混淆。",
      "守恒定律没有先判断系统和外力条件。",
      "电路等效前后物理量关系处理错误。",
    ],
    keywords: [
      {
        match: ["运动", "抛体", "圆周"],
        title: "运动学与曲线运动",
        points: [
          "基本量：位移、速度、加速度、时间、轨迹和参考系。",
          "匀变速直线运动重点掌握公式选择、v-t 图像面积和追及相遇。",
          "抛体运动按水平匀速、竖直匀变速分解。",
          "圆周运动关注线速度、角速度、周期、向心加速度和向心力来源。",
          "临界题常出现最高点、刚好通过、刚好脱离、恰好相遇。",
        ],
      },
      {
        match: ["力", "牛顿", "相互作用", "运动和力"],
        title: "相互作用与动力学",
        points: [
          "受力分析按重力、弹力、摩擦力、外加力、电磁力顺序排查。",
          "牛顿第二定律连接合外力与加速度，方向是关键。",
          "连接体问题可整体法求外部量，隔离法求内部力。",
          "摩擦力先判断静/滑，再判断方向和大小。",
          "临界条件常由摩擦力最大、弹力为零、绳刚好拉直给出。",
        ],
      },
      {
        match: ["机械能", "动量", "振动", "机械波", "光"],
        title: "守恒、振动与波动",
        points: [
          "机械能守恒关注只有重力或弹力做功；非保守力做功改变机械能。",
          "动量守恒先判断系统外力冲量是否可忽略。",
          "简谐运动抓回复力、振幅、周期、相位和能量转化。",
          "机械波关注波速、频率、波长、相位差、干涉和衍射。",
          "光学关注折射、全反射、干涉、衍射、偏振和光路可逆。",
        ],
      },
      {
        match: ["静电", "电路", "电能", "磁场", "电磁感应", "交变电流"],
        title: "电磁学主线",
        points: [
          "静电场抓电场强度、电势、电势能、电场线和等势面。",
          "电路抓欧姆定律、串并联、闭合电路、功率和动态分析。",
          "磁场抓洛伦兹力、安培力、带电粒子圆周运动。",
          "电磁感应抓磁通量变化、楞次定律、法拉第定律和能量转化。",
          "交变电流抓有效值、峰值、周期频率、变压器和远距离输电。",
        ],
      },
      {
        match: ["分子", "热力学", "原子", "原子核", "物态变化"],
        title: "热学与近代物理",
        points: [
          "分子动理论联系温度、内能、分子平均动能和微观模型。",
          "热力学第一定律关注做功、热传递和内能变化的符号约定。",
          "物态变化关注熔化、汽化、液化、升华、凝华与能量交换。",
          "原子结构掌握能级跃迁、光谱和玻尔模型。",
          "原子核关注核反应、半衰期、质能方程和核能利用。",
        ],
      },
    ],
  },
  化学: {
    aliases: ["化学"],
    objectives: [
      "围绕「{unit}」建立物质类别、结构性质、反应规律、实验现象和应用场景。",
      "用微粒观、守恒观、平衡观、分类观和变化观统摄化学知识。",
      "训练从宏观现象到微观解释、从符号方程到定量计算的转换。",
      "掌握实验设计、现象解释、离子方程、反应机理和工艺流程分析。",
    ],
    core: [
      "物质分类：单质、氧化物、酸碱盐、有机物、高分子等。",
      "结构决定性质：原子结构、化学键、分子构型、晶体类型影响物理化学性质。",
      "反应规律：氧化还原、离子反应、热效应、速率、平衡、电化学。",
      "实验证据：颜色、沉淀、气体、温度、pH、导电性和定量数据。",
      "应用：资源利用、材料合成、环境治理、生命健康和工业流程。",
    ],
    methods: [
      "分类建网：按元素、价态、物质类别和反应类型整理。",
      "守恒列式：质量守恒、元素守恒、电子守恒、电荷守恒、能量守恒。",
      "方程规范：化学方程式、离子方程式、电极反应式条件要完整。",
      "实验推断：先看操作目的，再解释现象和排除干扰。",
      "流程题：原料—预处理—反应—分离—循环—环保逐步拆解。",
    ],
    exam: [
      "选择题常考概念辨析、离子共存、物质转化、实验正误。",
      "推断题常考元素位置、价态变化、特征反应和现象链。",
      "实验题常考装置作用、试剂选择、误差分析和安全规范。",
      "原理题常考速率平衡、热化学、电化学和图像分析。",
      "有机题常考官能团、同分异构、反应类型和合成路线。",
    ],
    errors: [
      "方程式未配平，离子方程漏电荷守恒。",
      "忽略反应条件，导致产物判断错误。",
      "把现象和结论混为一谈。",
      "平衡移动只背结论，不分析浓度、压强、温度和催化剂作用。",
    ],
    keywords: [
      {
        match: ["实验"],
        title: "化学实验体系",
        points: [
          "基本操作：称量、溶解、过滤、蒸发、萃取、蒸馏、滴定。",
          "装置分析：发生、净化、干燥、收集、尾气处理各有功能。",
          "变量控制：试剂浓度、温度、接触面积、催化剂和时间。",
          "安全规范：防倒吸、防爆沸、防污染、防有毒气体逸散。",
          "结论表达：现象必须支持结论，必要时设置对照实验。",
        ],
      },
      {
        match: ["物质", "金属", "非金属", "元素周期"],
        title: "元素化合物与周期律",
        points: [
          "金属关注还原性、与水/酸/盐反应、氧化物与氢氧化物性质。",
          "非金属关注氧化性、酸性氧化物、含氧酸和环境影响。",
          "周期律关注原子半径、金属性、非金属性、最高价氧化物水化物酸碱性。",
          "元素推断依靠位置、价态、特征颜色、沉淀、气体和反应条件。",
          "知识网络要按“单质—氧化物—酸碱盐—转化关系”整理。",
        ],
      },
      {
        match: ["速率", "平衡", "离子", "水溶液"],
        title: "反应原理：速率、平衡与溶液",
        points: [
          "速率受浓度、温度、压强、催化剂、接触面积影响。",
          "化学平衡关注可逆反应、平衡常数、转化率和勒夏特列原理。",
          "水溶液平衡包括弱电解质电离、水解、沉淀溶解和酸碱中和。",
          "图像题重点读坐标、趋势、拐点、平台和条件变化。",
          "计算题常用三段式、守恒关系和近似判断。",
        ],
      },
      {
        match: ["氧化还原", "电化学", "热效应", "能量"],
        title: "氧化还原、电化学与能量",
        points: [
          "氧化还原抓化合价变化、电子转移、氧化剂还原剂。",
          "配平常用化合价升降法和电子守恒。",
          "原电池关注自发氧化还原、正负极、电解质、盐桥和电流方向。",
          "电解池关注外加电源、阴阳极产物和放电顺序。",
          "热化学关注焓变、键能、盖斯定律和能源利用。",
        ],
      },
      {
        match: ["有机", "烃", "衍生物", "生物大分子", "高分子"],
        title: "有机化学与材料",
        points: [
          "官能团决定性质：碳碳双键、卤素、羟基、醛基、羧基、酯基、氨基。",
          "反应类型：取代、加成、消去、氧化、还原、酯化、聚合。",
          "同分异构要按碳架、位置、官能团类别系统枚举。",
          "合成路线要倒推目标官能团，再选择保护、转化和成链策略。",
          "高分子关注单体、链节、聚合方式、材料性能和环保问题。",
        ],
      },
    ],
  },
  生物: {
    aliases: ["生物"],
    objectives: [
      "围绕「{unit}」建立结构与功能、物质与能量、遗传与进化、稳态与调节、生态与工程的知识网络。",
      "用生命观念解释事实，用科学思维分析图表，用实验探究验证假设。",
      "掌握概念边界、过程顺序、调控机制和变量关系。",
      "训练从文字、模式图、曲线图、表格到结论的综合转化。",
    ],
    core: [
      "结构功能观：细胞器、组织器官、系统结构与生命活动相适应。",
      "物质能量观：水、无机盐、糖类、脂质、蛋白质、核酸与代谢供能。",
      "遗传信息观：DNA、基因、染色体、表达、变异和进化。",
      "稳态调节观：神经、体液、免疫、植物激素共同维持稳定。",
      "生态工程观：种群、群落、生态系统、环境保护与生物技术应用。",
    ],
    methods: [
      "流程图法：把复杂生命过程拆成物质变化、能量变化、信息变化。",
      "对照实验：明确自变量、因变量、无关变量、对照组和重复原则。",
      "图表分析：先看坐标和变量，再看趋势、拐点、极值和原因。",
      "模型解释：用模式图解释过程，不只背结论。",
      "概念辨析：区分结构、功能、过程、结果和意义。",
    ],
    exam: [
      "选择题常考概念边界、过程顺序、实验判断和图像信息。",
      "非选择题常考机制解释、实验设计、遗传推理、生态分析。",
      "遗传题重基因型、表现型、分离定律、自由组合和伴性遗传。",
      "调节题重反馈调节、信号传递、效应器和稳态维持。",
      "生态题重数量关系、能量流动、物质循环和人类活动影响。",
    ],
    errors: [
      "把结构名称背熟但不能解释功能。",
      "实验题漏控制变量或没有设置对照。",
      "遗传推理不写亲本、配子、子代和概率过程。",
      "生态图像只描述趋势，不解释机制。",
    ],
    keywords: [
      {
        match: ["细胞", "分子", "生命历程", "物质输入", "能量供应"],
        title: "细胞与分子基础",
        points: [
          "细胞结构：细胞膜、细胞质、细胞核、细胞器的分工协作。",
          "分子组成：蛋白质和核酸是结构与信息的核心。",
          "物质运输：自由扩散、协助扩散、主动运输、胞吞胞吐。",
          "能量代谢：酶、ATP、细胞呼吸、光合作用及影响因素。",
          "生命历程：细胞增殖、分化、衰老、凋亡、癌变的区别。",
        ],
      },
      {
        match: ["遗传", "基因", "染色体", "突变", "进化"],
        title: "遗传与进化",
        points: [
          "遗传规律：分离定律、自由组合定律、伴性遗传和系谱分析。",
          "基因本质：DNA 复制、转录、翻译和中心法则。",
          "变异类型：基因突变、基因重组、染色体变异。",
          "进化机制：可遗传变异、自然选择、隔离、种群基因频率变化。",
          "解题路径：写清基因符号、亲本组合、配子比例和子代概率。",
        ],
      },
      {
        match: ["稳态", "神经", "体液", "免疫", "激素", "调节"],
        title: "稳态与调节",
        points: [
          "内环境稳态：成分、理化性质、反馈调节和稳态失衡。",
          "神经调节：反射弧、兴奋传导、突触传递和神经系统分级调节。",
          "体液调节：激素分泌、靶细胞识别、反馈调节和协同/拮抗。",
          "免疫调节：非特异性免疫、体液免疫、细胞免疫和免疫失调。",
          "植物调节：生长素、赤霉素、细胞分裂素、脱落酸、乙烯。",
        ],
      },
      {
        match: ["种群", "群落", "生态", "环境"],
        title: "生态系统",
        points: [
          "种群：数量特征、增长模型、密度制约和调查方法。",
          "群落：物种组成、种间关系、空间结构和演替。",
          "生态系统：组成成分、食物链网、能量流动、物质循环、信息传递。",
          "稳定性：抵抗力稳定性、恢复力稳定性及影响因素。",
          "生态保护：生物多样性、污染治理、生态工程和可持续发展。",
        ],
      },
      {
        match: ["发酵", "细胞工程", "基因工程", "生物技术"],
        title: "生物技术与工程",
        points: [
          "发酵工程：菌种选育、培养基、灭菌、发酵条件和产物分离。",
          "细胞工程：植物组织培养、动物细胞培养、细胞融合和胚胎工程。",
          "基因工程：目的基因、载体、工具酶、转化、筛选和表达检测。",
          "安全伦理：转基因、生物安全、医学伦理和社会影响。",
          "题目常要求把技术流程与原理、条件、结果对应起来。",
        ],
      },
    ],
  },
  历史: {
    aliases: ["历史"],
    objectives: [
      "围绕「{unit}」建立时间线、空间范围、关键事件、制度结构和历史解释。",
      "用唯物史观、时空观念、史料实证、历史解释和家国情怀组织学习。",
      "能从政治、经济、文化、社会、国际关系多维度分析历史现象。",
      "训练材料题的概括、比较、原因影响、评价和论证能力。",
    ],
    core: [
      "时空定位：明确时代背景、地域范围、阶段特征和前后联系。",
      "核心线索：制度变迁、经济发展、思想文化、民族国家、国际格局。",
      "因果分析：从生产力、阶级关系、政治制度、思想观念、外部环境切入。",
      "史料解读：区分史实、观点、立场、目的和史料局限。",
      "历史评价：坚持一分为二、放回时代、看长期影响。",
    ],
    methods: [
      "时间轴：把事件按阶段、转折点、高潮、结果排列。",
      "比较表：从背景、内容、特点、影响、局限进行横纵比较。",
      "材料三问：材料说了什么、为什么这样说、能说明什么问题。",
      "小论文：观点明确、史实充分、论证成段、结论回扣。",
      "概念卡：制度、革命、工业化、全球化、民族主义等要准确界定。",
    ],
    exam: [
      "选择题常考阶段特征、因果关系、材料主旨和概念辨析。",
      "材料题常考概括信息、比较异同、分析原因影响、评价观点。",
      "开放题重史实支撑和逻辑论证。",
      "中外关联题重同一时期不同地区的互动和差异。",
      "选修内容常从制度治理、经济生活、文化交流角度设问。",
    ],
    errors: [
      "脱离时代背景用现代标准简单评价古人古事。",
      "只罗列史实，不回答设问的原因、特点或影响。",
      "材料题照抄材料，缺少概括和转化。",
      "把相近概念混淆，如民主、共和、宪政、法治。",
    ],
    keywords: [
      {
        match: ["政治制度", "国家制度", "法律", "基层治理", "社会保障", "官员"],
        title: "制度史与国家治理",
        points: [
          "制度形成受经济基础、社会结构、思想观念和权力关系影响。",
          "中国古代重点抓中央集权、官僚制度、选官制度和法律教化。",
          "西方政治制度重点抓城邦、共和、议会、宪政和权力制衡。",
          "治理评价要看效率、公平、稳定、参与和历史局限。",
          "材料题常要求从制度设计和治理效果两个层面作答。",
        ],
      },
      {
        match: ["资本主义", "工业革命", "社会主义", "革命", "两次世界大战", "十月革命"],
        title: "近现代转型与革命",
        points: [
          "资本主义发展线索：商业扩张、制度确立、工业革命、殖民体系。",
          "革命分析要抓社会矛盾、阶级力量、思想理论、组织形式和结果。",
          "工业革命影响政治、经济、社会结构、城市化和世界市场。",
          "世界大战与国际秩序重在原因、过程、结果和格局重塑。",
          "社会主义运动重点理解理论、实践、制度探索和历史影响。",
        ],
      },
      {
        match: ["经济", "商业", "贸易", "赋税", "货币", "食物", "交通", "生产工具"],
        title: "经济与社会生活",
        points: [
          "经济史抓生产力、生产关系、市场、技术、政策和生活方式。",
          "商业贸易联系城市、交通、货币、信用、制度和全球联系。",
          "赋税制度反映国家财政、社会控制和经济结构变化。",
          "社会生活材料题常从衣食住行、医疗、公共卫生、城市乡村切入。",
          "评价经济政策要兼顾短期效果和长期结构性影响。",
        ],
      },
      {
        match: ["文化", "文明", "文化交流", "传播", "人口迁徙", "商路"],
        title: "文明交流与文化认同",
        points: [
          "文明发展关注自然环境、经济形态、政治制度和思想文化。",
          "文化交流方式包括战争、贸易、移民、宗教、技术传播和教育。",
          "人口迁徙影响族群结构、文化融合、冲突与认同。",
          "文化传承既有连续性，也有选择、改造和创新。",
          "答题要避免“单向传播”思维，注意互动与互鉴。",
        ],
      },
    ],
  },
  地理: {
    aliases: ["地理"],
    objectives: [
      "围绕「{unit}」建立地理位置、要素组成、过程机制、区域差异和人地关系。",
      "用综合思维、区域认知、地理实践力和人地协调观分析问题。",
      "训练图表判读、因果链构建、区位评价和区域治理表达。",
      "把自然地理机制与人文地理活动连接成完整系统。",
    ],
    core: [
      "位置与尺度：经纬度、海陆位置、地形区、气候区和区域尺度。",
      "自然要素：地貌、气候、水文、土壤、植被相互作用。",
      "人文要素：人口、城市、产业、交通、资源、环境和政策。",
      "过程机制：运动、循环、迁移、演替、集聚、扩散和反馈。",
      "治理路径：问题识别、原因分析、影响评价、措施设计。",
    ],
    methods: [
      "读图五步：图名、图例、坐标、数值、空间分布。",
      "因果链：自然条件—人类活动—环境影响—反馈调控。",
      "区位分析：资源、市场、交通、劳动力、技术、政策、生态约束。",
      "区域比较：位置、自然、人文、发展阶段和问题差异。",
      "措施表达：工程、技术、管理、政策、教育和国际合作分层写。",
    ],
    exam: [
      "选择题常考图像判读、过程机制、区位因素和区域差异。",
      "综合题常考成因分析、影响评价、措施建议和开放探究。",
      "自然地理重机制，人文地理重区位与协调。",
      "区域题重材料信息提取和迁移应用。",
      "国家安全主题常联系资源、生态、环境和全球治理。",
    ],
    errors: [
      "只背结论，不会根据图文材料重建因果链。",
      "自然原因和人为原因混写，层次不清。",
      "区位题漏生态环境和政策约束。",
      "措施题空泛，缺少针对性和可操作性。",
    ],
    keywords: [
      {
        match: ["地球", "运动", "宇宙"],
        title: "地球运动与空间定位",
        points: [
          "地球运动包括自转、公转、黄赤交角及其地理意义。",
          "昼夜长短、正午太阳高度、季节变化和五带划分是核心应用。",
          "时间计算关注地方时、区时、日期变更线和太阳高度。",
          "空间定位要结合经纬网、海陆轮廓、地形气候和人文特征。",
        ],
      },
      {
        match: ["大气", "水", "地表形态", "植被", "土壤", "自然环境"],
        title: "自然地理过程",
        points: [
          "大气运动抓热力差异、气压带风带、季风、天气系统和气候成因。",
          "水循环抓蒸发、降水、径流、下渗、地下水和人类影响。",
          "地貌塑造由内力与外力共同作用，关注板块、流水、风、冰川、海浪。",
          "植被土壤受气候、地形、母质、生物和人类活动影响。",
          "自然环境整体性强调一个要素变化会引发连锁反馈。",
        ],
      },
      {
        match: ["人口", "乡村", "城镇", "产业", "交通"],
        title: "人文地理与区位",
        points: [
          "人口分析关注数量、结构、迁移、分布和人口问题。",
          "城镇化关注动力、空间形态、功能分区、问题和治理。",
          "产业区位要分农业、工业、服务业分别分析主导因素。",
          "交通运输影响区域联系、产业布局、城市发展和资源开发。",
          "乡村振兴强调产业、生态、文化、治理和生活改善。",
        ],
      },
      {
        match: ["区域", "区域发展", "协调", "生态环境建设"],
        title: "区域发展与协调",
        points: [
          "区域特征包括位置、自然基础、资源禀赋、产业结构和发展阶段。",
          "区域发展问题常体现资源约束、产业转型、生态退化和城乡差异。",
          "协调发展关注流域、城市群、产业转移、东西部协作和区域分工。",
          "生态建设需结合退耕还林、水土保持、防沙治沙、湿地保护等措施。",
          "评价区域方案要兼顾经济效益、社会效益和生态效益。",
        ],
      },
      {
        match: ["资源", "环境", "国家安全", "全球环境"],
        title: "资源环境与国家安全",
        points: [
          "自然资源安全关注数量、质量、空间分布、开发利用和战略保障。",
          "生态安全关注生物多样性、水土保持、荒漠化、湿地和森林。",
          "环境治理关注污染源、扩散路径、影响对象和治理体系。",
          "全球环境治理强调共同但有区别的责任、国际合作和技术转移。",
          "答案要体现安全底线、可持续利用和系统治理。",
        ],
      },
    ],
  },
  思想政治: {
    aliases: ["思想政治", "政治"],
    objectives: [
      "围绕「{unit}」建立概念体系、理论逻辑、制度依据、现实案例和答题框架。",
      "用政治认同、科学精神、法治意识、公共参与四大核心素养组织学习。",
      "训练材料信息提取、理论匹配、逻辑展开和规范表述。",
      "把教材语言转化为可迁移的时政分析和主观题表达。",
    ],
    core: [
      "概念：先明确主体、对象、条件、作用和边界。",
      "原理：理解理论前提、逻辑关系、价值目标和实践要求。",
      "制度：把党的领导、人民当家作主、依法治国等放入整体框架。",
      "材料：提取行为主体、问题情境、政策措施、结果影响。",
      "表达：理论点 + 材料扣合 + 逻辑连接 + 价值升华。",
    ],
    methods: [
      "关键词定位：根据材料中的主体和动词匹配教材知识。",
      "层级作答：是什么、为什么、怎么办、意义影响分层写。",
      "时政迁移：把热点材料转化为教材概念和原理。",
      "辩证分析：既看主要矛盾，也看矛盾双方和发展过程。",
      "法治表达：权利义务、程序规则、主体责任要说清楚。",
    ],
    exam: [
      "选择题重概念边界、主体对应、因果关系和表述正误。",
      "主观题重材料解读、理论匹配、分点规范和逻辑完整。",
      "经济题常考市场、政府、分配、发展理念和宏观政策。",
      "政治法治题常考党的领导、人民民主、依法治国和国家治理。",
      "哲学题常考唯物论、认识论、辩证法、历史唯物主义。",
    ],
    errors: [
      "背了理论但不会扣材料。",
      "主体错位，如把政府、党、人大、公民职责混用。",
      "哲学题只写原理名称，不写方法论和材料分析。",
      "法律题忽略权利义务统一和程序意识。",
    ],
    keywords: [
      {
        match: ["社会主义", "中国特色社会主义", "新时代"],
        title: "中国特色社会主义主线",
        points: [
          "理解科学社会主义从理论到实践的发展逻辑。",
          "把中国革命、建设、改革、新时代放进历史连续性中理解。",
          "核心问题：为什么只有社会主义才能救中国、发展中国。",
          "新时代重点关注主要矛盾变化、战略安排、青年责任和民族复兴。",
        ],
      },
      {
        match: ["经济", "市场", "收入", "社会保障", "发展"],
        title: "经济与社会",
        points: [
          "生产资料所有制决定基本经济制度的重要基础。",
          "市场在资源配置中起决定性作用，更好发挥政府作用。",
          "高质量发展关注创新、协调、绿色、开放、共享。",
          "分配与社会保障体现效率与公平、发展与共享的统一。",
          "材料题要区分企业、市场、政府、劳动者和社会组织主体。",
        ],
      },
      {
        match: ["政治", "法治", "人民", "共产党", "民族区域"],
        title: "政治与法治",
        points: [
          "党的领导是中国特色社会主义最本质特征和最大优势。",
          "人民当家作主通过人民代表大会制度、协商民主、基层民主实现。",
          "全面依法治国强调科学立法、严格执法、公正司法、全民守法。",
          "民族区域自治制度要从统一多民族国家和共同体意识角度理解。",
          "主体职责要精准：党、人大、政府、法院、政协、公民不可混淆。",
        ],
      },
      {
        match: ["哲学", "世界", "规律", "认识", "价值", "文化"],
        title: "哲学与文化",
        points: [
          "唯物论关注物质与意识、规律客观性和主观能动性。",
          "认识论关注实践、认识、真理和认识发展过程。",
          "辩证法关注联系、发展、矛盾、量变质变、否定之否定。",
          "历史唯物主义关注社会存在、社会意识、人民群众和价值判断。",
          "文化题关注中华优秀传统文化创造性转化、创新性发展。",
        ],
      },
      {
        match: ["国际", "法律", "逻辑", "思维", "婚姻", "劳动", "知识产权"],
        title: "选择性必修专题",
        points: [
          "国际政治经济：国家利益、世界多极化、经济全球化和国际组织。",
          "法律生活：民事权利义务、合同侵权、婚姻家庭、劳动就业、诉讼程序。",
          "逻辑思维：概念、判断、推理、辩证思维和创新思维。",
          "专题题常以真实案例呈现，必须先识别法律关系或逻辑关系。",
          "答案要做到规则依据、事实对应、结论明确。",
        ],
      },
    ],
  },
};

const SUBJECT_RELATIONS = {
  语文: [
    {
      title: "阅读能力纵向链",
      nodes: ["现代文阅读", "文学类文本", "实用性阅读", "科学与文化论著", "外国作家作品"],
      note: "从信息获取、结构分析、语言赏析到跨文化比较，逐步升级阅读深度。",
    },
    {
      title: "古代诗文与传统文化链",
      nodes: ["古代诗文", "中华传统文化", "整本书阅读", "文化传承", "写作"],
      note: "文言基础支撑诗文赏析，传统文化积累最终迁移到议论文和文化类写作。",
    },
    {
      title: "革命传统与时代精神链",
      nodes: ["中国革命传统", "家国情怀", "人物精神", "现实表达"],
      note: "把历史语境、人物行动、精神品质和当代青年责任连成价值表达链。",
    },
    {
      title: "思辨表达与写作链",
      nodes: ["思辨性阅读", "概念界定", "论证结构", "材料整合", "写作单元"],
      note: "先读懂观点和逻辑，再完成审题立意、分层论证和材料扣合。",
    },
  ],
  数学: [
    {
      title: "集合—函数—导数主线",
      nodes: ["集合", "函数", "指数函数", "对数函数", "三角函数", "导数"],
      note: "集合语言提供定义域与逻辑基础，函数图像和性质最终在导数应用中综合。",
    },
    {
      title: "几何代数化主线",
      nodes: ["平面向量", "立体几何", "空间向量", "直线", "圆", "圆锥曲线"],
      note: "从几何直观进入向量和坐标表达，用代数方法处理空间与曲线问题。",
    },
    {
      title: "离散与统计决策链",
      nodes: ["计数原理", "概率", "随机变量", "统计", "成对数据"],
      note: "从计数到概率模型，再到随机变量和统计推断，形成数据分析能力。",
    },
    {
      title: "数列—函数—不等式综合链",
      nodes: ["数列", "函数的应用", "导数", "最值", "参数"],
      note: "数列递推、函数单调和导数最值经常在压轴题中互相嵌套。",
    },
  ],
  英语: [
    {
      title: "人与自我主题链",
      nodes: ["Friendship", "Sports", "Morals", "Healthy Lifestyle", "Career"],
      note: "从个人成长、健康生活到职业规划，构成应用文和读后续写的人物发展线。",
    },
    {
      title: "文化理解主题链",
      nodes: ["Travelling", "Cultural Heritage", "History", "Festivals", "Cultures", "Attractions"],
      note: "文化遗产、节日、历史传统和旅行介绍共享文化解释与推荐表达。",
    },
    {
      title: "科技未来主题链",
      nodes: ["Internet", "Space Exploration", "Scientists", "Future", "Science Fiction"],
      note: "科技说明文、未来生活和科学人物主题共享问题—方案—影响的语篇结构。",
    },
    {
      title: "环境与公共安全链",
      nodes: ["Natural Disasters", "Wildlife", "Environmental Protection", "First Aid", "Sharing"],
      note: "灾害、环保、急救与公益服务可迁移到倡议信、说明文和社会责任表达。",
    },
  ],
  物理: [
    {
      title: "运动—力—能量—动量主线",
      nodes: ["运动", "力", "牛顿", "抛体", "圆周", "机械能", "动量"],
      note: "运动描述提供状态量，力解释状态改变，能量和动量用于多过程综合。",
    },
    {
      title: "振动—波—光链",
      nodes: ["机械振动", "机械波", "光", "干涉", "衍射"],
      note: "简谐振动是波动基础，波动思想迁移到声、光和电磁波理解。",
    },
    {
      title: "电场—电路—磁场—电磁感应链",
      nodes: ["静电场", "电路", "电能", "磁场", "电磁感应", "交变电流"],
      note: "从场的作用进入电路能量，再到磁场受力和电磁感应能量转化。",
    },
    {
      title: "热学—原子核微观链",
      nodes: ["分子动理论", "热力学", "物态变化", "原子结构", "原子核"],
      note: "用微观模型解释宏观热现象，并延伸到近代物理的能级与核能。",
    },
  ],
  化学: [
    {
      title: "实验—物质分类—元素化合物链",
      nodes: ["实验", "化学物质", "金属", "非金属", "元素周期律"],
      note: "实验方法支撑物质鉴别，分类观帮助建立元素化合物转化网络。",
    },
    {
      title: "结构决定性质链",
      nodes: ["原子结构", "分子结构", "晶体结构", "元素周期律", "有机结构"],
      note: "微观结构解释物理性质、化学性质和材料性能，是选修二与有机的桥梁。",
    },
    {
      title: "反应原理综合链",
      nodes: ["热效应", "速率", "平衡", "离子反应", "氧化还原", "电化学"],
      note: "化学反应从能量、快慢、限度、微粒和电子转移多个角度综合分析。",
    },
    {
      title: "有机合成与材料链",
      nodes: ["有机化合物", "烃", "烃的衍生物", "生物大分子", "合成高分子"],
      note: "官能团转化、碳骨架构建和聚合反应共同支撑有机合成路线。",
    },
  ],
  生物: [
    {
      title: "细胞结构—代谢—生命历程链",
      nodes: ["走近细胞", "组成细胞", "细胞结构", "物质输入", "能量供应", "生命历程"],
      note: "结构决定功能，物质和能量代谢支撑细胞增殖、分化和生命活动。",
    },
    {
      title: "遗传信息—变异—进化链",
      nodes: ["遗传因子", "染色体", "基因本质", "基因表达", "变异", "进化"],
      note: "从遗传规律到分子机制，再到变异来源和生物进化过程。",
    },
    {
      title: "稳态调节系统链",
      nodes: ["内环境", "神经调节", "体液调节", "免疫调节", "植物激素"],
      note: "动物和植物都通过信息传递与反馈调节维持生命活动稳定。",
    },
    {
      title: "生态—工程—安全伦理链",
      nodes: ["种群", "群落", "生态系统", "生态保护", "发酵工程", "细胞工程", "基因工程"],
      note: "生态规律支撑环境治理，生物技术应用必须同时考虑效率、安全和伦理。",
    },
  ],
  历史: [
    {
      title: "中国制度变迁链",
      nodes: ["古代中国政治制度", "官员选拔", "法律与教化", "基层治理", "社会保障"],
      note: "从国家权力组织到社会治理机制，理解中国国家治理传统与现代转型。",
    },
    {
      title: "世界文明—资本主义—全球化链",
      nodes: ["古代文明", "中古时期", "走向整体的世界", "资本主义制度", "工业革命", "世界殖民体系"],
      note: "世界联系由区域文明走向全球体系，资本主义扩张重塑国际秩序。",
    },
    {
      title: "革命与现代国家建构链",
      nodes: ["反侵略", "社会主义", "现代中国政治建设", "祖国统一", "对外关系"],
      note: "近现代中国历史以民族独立、国家建设、制度探索和国际关系展开。",
    },
    {
      title: "经济社会生活与文化交流链",
      nodes: ["食物生产", "生产工具", "商业贸易", "交通", "医疗", "文化交流", "文化保护"],
      note: "经济生活变化推动社会结构调整，文化交流塑造认同与文明互鉴。",
    },
  ],
  地理: [
    {
      title: "地球运动—大气—水—地貌自然链",
      nodes: ["宇宙中的地球", "地球的运动", "大气", "水", "地表形态", "植被", "土壤"],
      note: "地球运动决定能量分布，大气水文地貌共同塑造自然环境格局。",
    },
    {
      title: "人口—城镇—产业—交通人文链",
      nodes: ["人口", "乡村", "城镇", "产业", "交通运输", "区域发展"],
      note: "人口流动、城镇化、产业布局和交通网络共同决定区域发展路径。",
    },
    {
      title: "区域认知与协调发展链",
      nodes: ["区域", "区域发展", "区域协调", "生态环境建设"],
      note: "区域差异是分析起点，协调发展和生态修复是治理目标。",
    },
    {
      title: "资源环境与国家安全链",
      nodes: ["自然资源", "生态保护", "环境治理", "全球环境治理", "国家安全"],
      note: "资源供给、生态底线和环境治理共同构成国家安全的重要基础。",
    },
  ],
  思想政治: [
    {
      title: "中国特色社会主义历史逻辑链",
      nodes: ["科学社会主义", "救中国", "发展中国", "新时代"],
      note: "从理论源流、革命实践到改革发展和新时代，建立政治认同主线。",
    },
    {
      title: "经济制度—市场—发展—分配链",
      nodes: ["生产资料所有制", "社会主义市场经济", "经济发展", "收入分配", "社会保障"],
      note: "基本经济制度、市场与政府关系、高质量发展和共同富裕相互支撑。",
    },
    {
      title: "党的领导—人民民主—依法治国链",
      nodes: ["中国共产党的领导", "人民当家作主", "全面依法治国", "民族区域自治"],
      note: "政治与法治内容共同构成中国特色社会主义政治制度框架。",
    },
    {
      title: "哲学方法—文化传承—逻辑思维链",
      nodes: ["世界本质", "规律", "认识", "价值选择", "文化创新", "逻辑思维"],
      note: "哲学提供世界观和方法论，文化与逻辑模块用于提升判断和表达能力。",
    },
  ],
};

function normalizeSubjectKey(subjectName) {
  return Object.keys(SUBJECT_GUIDES).find((key) =>
    SUBJECT_GUIDES[key].aliases.some((alias) => subjectName.includes(alias)),
  );
}

function fillTemplate(text, subject, volume, unit) {
  return text
    .replaceAll("{subject}", subject.name)
    .replaceAll("{volume}", volume.name)
    .replaceAll("{unit}", unit);
}

function fillPoints(points, subject, volume, unit) {
  return points.map((point) => fillTemplate(point, subject, volume, unit));
}

function matchedKeywordBranches(guide, subject, volume, unit) {
  const text = `${subject.name} ${volume.name} ${unit}`;
  return guide.keywords
    .filter((branch) => branch.match.some((keyword) => text.includes(keyword)))
    .map((branch) => ({
      title: branch.title,
      points: fillPoints(branch.points, subject, volume, unit),
    }));
}

function buildMindMap(subjectIndex, volumeIndex, unitIndex) {
  const subject = COURSE[subjectIndex];
  const volume = subject.volumes[volumeIndex];
  const unit = volume.units[unitIndex];
  const specificBranches =
    typeof window !== "undefined" && typeof window.createSpecificMindMapBranches === "function"
      ? window.createSpecificMindMapBranches({
          subjectName: subject.name,
          volumeName: volume.name,
          unitName: unit,
        })
      : null;

  if (specificBranches && specificBranches.length) {
    return {
      title: unit,
      subtitle: `${subject.name} · ${volume.name}`,
      branches: specificBranches,
    };
  }

  const key = normalizeSubjectKey(subject.name);
  const guide = SUBJECT_GUIDES[key] || SUBJECT_GUIDES.语文;
  const keywordBranches = matchedKeywordBranches(guide, subject, volume, unit);
  const branches = [
    { title: "学习目标", points: fillPoints(guide.objectives, subject, volume, unit) },
    { title: "核心知识框架", points: fillPoints(guide.core, subject, volume, unit) },
    { title: "方法模型", points: fillPoints(guide.methods, subject, volume, unit) },
    { title: "常考题型与应用", points: fillPoints(guide.exam, subject, volume, unit) },
    { title: "易错点与诊断", points: fillPoints(guide.errors, subject, volume, unit) },
    ...keywordBranches,
  ];

  if (!keywordBranches.length) {
    branches.push({
      title: "本单元专题深化",
      points: [
        `围绕「${unit}」建立概念卡、例题卡、错题卡和迁移应用卡四类资料。`,
        `把「${volume.name}」前后单元联系起来，寻找知识递进、方法复用和综合命题入口。`,
        `至少完成一次“知识点—例题—变式—错因—总结”的闭环复盘。`,
        `用 3 分钟口头讲解本单元主线，检验自己是否真正理解。`,
      ],
    });
  }

  return {
    title: unit,
    subtitle: `${subject.name} · ${volume.name}`,
    branches,
  };
}

function countMindMapNodes(map) {
  return 1 + map.branches.reduce((sum, branch) => sum + 1 + branch.points.length, 0);
}

function countAllKnowledgeNodes() {
  return COURSE.reduce(
    (subjectSum, subject, subjectIndex) =>
      subjectSum +
      subject.volumes.reduce(
        (volumeSum, volume, volumeIndex) =>
          volumeSum +
          volume.units.reduce(
            (unitSum, _unit, unitIndex) => unitSum + countMindMapNodes(buildMindMap(subjectIndex, volumeIndex, unitIndex)),
            0,
          ),
        0,
      ),
    0,
  );
}

function allUnitsForSubject(subjectIndex) {
  const subject = COURSE[subjectIndex];
  return subject.volumes.flatMap((volume, volumeIndex) =>
    volume.units.map((unit, unitIndex) => ({
      subjectIndex,
      volumeIndex,
      unitIndex,
      volumeName: volume.name,
      unit,
      text: `${subject.name} ${volume.name} ${unit}`,
    })),
  );
}

function relationMatches(subjectIndex, keywords) {
  return allUnitsForSubject(subjectIndex).filter((item) =>
    keywords.some((keyword) => item.text.toLowerCase().includes(String(keyword).toLowerCase())),
  );
}

function topicGroupsForSubject(subjectIndex) {
  const subject = COURSE[subjectIndex];
  const guideKey = normalizeSubjectKey(subject.name);
  const guide = SUBJECT_GUIDES[guideKey] || SUBJECT_GUIDES.语文;
  return guide.keywords
    .map((topic) => ({
      title: topic.title,
      keywords: topic.match,
      units: relationMatches(subjectIndex, topic.match),
      points: fillPoints(topic.points, subject, { name: "全册贯通" }, subject.name),
    }))
    .filter((topic) => topic.units.length);
}

function subjectRelations(subjectIndex) {
  const subject = COURSE[subjectIndex];
  const guideKey = normalizeSubjectKey(subject.name);
  const relations = SUBJECT_RELATIONS[guideKey] || [];
  return relations.map((relation) => ({
    ...relation,
    units: relationMatches(subjectIndex, relation.nodes),
  }));
}

function countSubjectNetworkNodes(subjectIndex) {
  const subject = COURSE[subjectIndex];
  const guideKey = normalizeSubjectKey(subject.name);
  const guide = SUBJECT_GUIDES[guideKey] || SUBJECT_GUIDES.语文;
  const unitCount = allUnitsForSubject(subjectIndex).length;
  const relationCount = subjectRelations(subjectIndex).reduce((sum, relation) => sum + 1 + relation.nodes.length + relation.units.length, 0);
  const topicCount = topicGroupsForSubject(subjectIndex).reduce((sum, topic) => sum + 1 + topic.units.length + topic.points.length, 0);
  return (
    1 +
    subject.volumes.length +
    unitCount +
    relationCount +
    topicCount +
    guide.core.length +
    guide.methods.length +
    guide.exam.length +
    guide.errors.length
  );
}

const state = {
  subjectIndex: 0,
  volumeIndex: 0,
  query: "",
  mode: "catalog",
};

const WORD_TRAINER_STORAGE_KEY = `${STORAGE_KEY}-english-word-trainer-v1`;

const ENGLISH_WORD_BANK_FALLBACK = [
  { word: "acquire", meaning: "获得；习得", phrase: "acquire knowledge", example: "Students acquire confidence through steady practice.", mnemonic: "a + quire 像“去取”：主动去取知识就是 acquire。", category: "高考核心" },
  { word: "adapt", meaning: "适应；改编", phrase: "adapt to change", example: "We must adapt to a rapidly changing world.", mnemonic: "ad + apt：apt 有合适感，让自己变合适就是适应。", category: "高考核心" },
  { word: "advocate", meaning: "提倡；支持者", phrase: "advocate reading", example: "The article advocates a balanced lifestyle.", mnemonic: "ad + voc 声音：把声音说出去支持某事。", category: "写作表达" },
  { word: "ambition", meaning: "抱负；雄心", phrase: "a strong ambition", example: "Her ambition is to become a scientist.", mnemonic: "ambi 像“两边都想要”，目标很大就是雄心。", category: "人物品质" },
  { word: "analyze", meaning: "分析", phrase: "analyze data", example: "The ability to analyze problems matters in exams.", mnemonic: "ana 分开 + lyze：把整体拆开看。", category: "学术阅读" },
  { word: "approach", meaning: "方法；接近", phrase: "an effective approach", example: "This approach helps us memorize words faster.", mnemonic: "ap + proach 接近：接近目标的路就是方法。", category: "学术阅读" },
  { word: "assume", meaning: "假定；承担", phrase: "assume responsibility", example: "Do not assume the answer before reading carefully.", mnemonic: "as + sume 拿起：先拿来当假设，也能拿起责任。", category: "熟词僻义" },
  { word: "available", meaning: "可获得的；有空的", phrase: "available resources", example: "Online courses are available to most students.", mnemonic: "avail 有用 + able：能用得到的。", category: "高考核心" },
  { word: "beneficial", meaning: "有益的", phrase: "be beneficial to", example: "Regular exercise is beneficial to learning efficiency.", mnemonic: "benefit 益处 + cial：有益性质。", category: "写作表达" },
  { word: "challenge", meaning: "挑战；质疑", phrase: "face a challenge", example: "Every challenge can become a chance to grow.", mnemonic: "考试常写：challenge → chance，挑战变机会。", category: "写作表达" },
  { word: "commit", meaning: "投入；承诺；犯", phrase: "commit oneself to", example: "He committed himself to improving his English.", mnemonic: "com 一起 + mit 送出：把自己送进去就是投入。", category: "高考核心" },
  { word: "consequence", meaning: "结果；后果", phrase: "serious consequences", example: "Every choice has its consequence.", mnemonic: "con + sequence 顺序：顺着发生的结果。", category: "学术阅读" },
  { word: "consistent", meaning: "持续一致的", phrase: "consistent effort", example: "Consistent review beats last-minute cramming.", mnemonic: "consist 组成稳定，行为稳定就是一致。", category: "写作表达" },
  { word: "contribute", meaning: "贡献；导致", phrase: "contribute to society", example: "Reading contributes to better writing.", mnemonic: "con + tribute 给予：一起给予就是贡献。", category: "高考核心" },
  { word: "crucial", meaning: "至关重要的", phrase: "a crucial factor", example: "Vocabulary is crucial for reading comprehension.", mnemonic: "crux 十字路口：关键路口就是 crucial。", category: "写作表达" },
  { word: "demonstrate", meaning: "展示；证明", phrase: "demonstrate ability", example: "The experiment demonstrates the rule clearly.", mnemonic: "demo 展示 + strate：展示出来证明。", category: "学术阅读" },
  { word: "determine", meaning: "决定；下定决心", phrase: "be determined to", example: "She is determined to master 20 words a day.", mnemonic: "term 边界：把边界定下来就是决定。", category: "高考核心" },
  { word: "efficient", meaning: "高效的", phrase: "efficient learning", example: "A good plan makes vocabulary learning efficient.", mnemonic: "effect 效果 + ient：有效率的。", category: "写作表达" },
  { word: "enhance", meaning: "提高；增强", phrase: "enhance memory", example: "Examples can enhance word memory.", mnemonic: "en 使 + hance 高：使变高，增强。", category: "写作表达" },
  { word: "evaluate", meaning: "评价；评估", phrase: "evaluate progress", example: "We should evaluate our learning every week.", mnemonic: "value 价值：评估就是判断价值。", category: "学术阅读" },
  { word: "evidence", meaning: "证据", phrase: "strong evidence", example: "Use evidence to support your opinion.", mnemonic: "evident 明显的；让观点明显的材料就是证据。", category: "学术阅读" },
  { word: "expand", meaning: "扩大；拓展", phrase: "expand vocabulary", example: "Reading helps expand vocabulary naturally.", mnemonic: "ex 向外 + pand 展开：向外展开。", category: "高考核心" },
  { word: "factor", meaning: "因素", phrase: "key factors", example: "Sleep is a key factor in learning.", mnemonic: "fact 事实：影响事实的要素。", category: "学术阅读" },
  { word: "flexible", meaning: "灵活的", phrase: "a flexible plan", example: "A flexible schedule reduces stress.", mnemonic: "flex 弯曲：能弯能变就是灵活。", category: "写作表达" },
  { word: "generate", meaning: "产生；生成", phrase: "generate ideas", example: "Discussion can generate new ideas.", mnemonic: "gen 产生：generate 就是生成。", category: "学术阅读" },
  { word: "identify", meaning: "识别；确认", phrase: "identify the main idea", example: "First identify the topic sentence.", mnemonic: "identity 身份：确认身份就是识别。", category: "阅读题型" },
  { word: "impact", meaning: "影响；冲击", phrase: "have an impact on", example: "Technology has a great impact on education.", mnemonic: "im + pact 碰撞：撞上就产生影响。", category: "写作表达" },
  { word: "maintain", meaning: "维持；保持", phrase: "maintain balance", example: "Students need to maintain a healthy routine.", mnemonic: "main 手 + tain 拿住：一直拿住就是保持。", category: "高考核心" },
  { word: "obtain", meaning: "获得", phrase: "obtain information", example: "We obtain information from reliable sources.", mnemonic: "ob + tain 拿到：正式获得。", category: "学术阅读" },
  { word: "participate", meaning: "参与", phrase: "participate in activities", example: "Students should participate in class actively.", mnemonic: "part 部分：成为其中一部分就是参与。", category: "校园生活" },
  { word: "perspective", meaning: "视角；观点", phrase: "from another perspective", example: "Try to see the problem from a new perspective.", mnemonic: "spect 看：看问题的角度。", category: "写作表达" },
  { word: "potential", meaning: "潜力；潜在的", phrase: "realize one's potential", example: "Everyone has the potential to improve.", mnemonic: "potent 有力：还没释放出来的力就是潜力。", category: "人物品质" },
  { word: "priority", meaning: "优先事项", phrase: "top priority", example: "Health should be a top priority.", mnemonic: "prior 在前：放在前面的事。", category: "写作表达" },
  { word: "promote", meaning: "促进；推广", phrase: "promote development", example: "Reading promotes critical thinking.", mnemonic: "pro 向前 + mote 推：向前推动。", category: "写作表达" },
  { word: "reliable", meaning: "可靠的", phrase: "reliable information", example: "Choose reliable sources when doing research.", mnemonic: "rely 依靠 + able：可依靠的。", category: "学术阅读" },
  { word: "significant", meaning: "重要的；显著的", phrase: "a significant change", example: "Small habits can make a significant difference.", mnemonic: "sign 标志：有明显标志的就是显著的。", category: "写作表达" },
  { word: "strategy", meaning: "策略", phrase: "learning strategies", example: "Spaced review is a useful strategy.", mnemonic: "strateg 像“战略”：学习也要有策略。", category: "学习方法" },
  { word: "sufficient", meaning: "足够的", phrase: "sufficient time", example: "Sufficient sleep improves memory.", mnemonic: "suf 下面 + fic 做：做到足够。", category: "高考核心" },
  { word: "transform", meaning: "转变；改造", phrase: "transform pressure into power", example: "Good habits transform effort into progress.", mnemonic: "trans 跨过 + form 形状：跨形态改变。", category: "写作表达" },
  { word: "account", meaning: "账户；叙述；解释", phrase: "take account of", example: "We should take account of different opinions.", mnemonic: "count 是数，account 是把事情“数清楚、说清楚”。", category: "熟词僻义" },
  { word: "achieve", meaning: "实现；取得", phrase: "achieve a goal", example: "You can achieve progress through daily review.", mnemonic: "a + chieve 像“去抵达”：抵达目标就是实现。", category: "写作表达" },
  { word: "acknowledge", meaning: "承认；感谢", phrase: "acknowledge the truth", example: "We must acknowledge our mistakes before improving.", mnemonic: "a + knowledge：知道并说出来，就是承认。", category: "学术阅读" },
  { word: "adequate", meaning: "足够的；合格的", phrase: "adequate preparation", example: "Adequate preparation reduces exam anxiety.", mnemonic: "ad + equate 相等：数量够到标准。", category: "高考核心" },
  { word: "affect", meaning: "影响", phrase: "affect learning", example: "Sleep quality affects memory.", mnemonic: "effect 是结果，affect 是造成结果的动作。", category: "易混词" },
  { word: "alternative", meaning: "可替代的；选择", phrase: "an alternative solution", example: "Online learning offers an alternative way to study.", mnemonic: "alter 改变：换一种选择。", category: "学术阅读" },
  { word: "arrange", meaning: "安排；整理", phrase: "arrange a schedule", example: "Arrange your review time before the exam.", mnemonic: "range 排列范围：把事情排好。", category: "校园生活" },
  { word: "aspect", meaning: "方面；层面", phrase: "every aspect of life", example: "Vocabulary affects every aspect of English learning.", mnemonic: "spect 看：从某个角度看就是方面。", category: "学术阅读" },
  { word: "attitude", meaning: "态度", phrase: "a positive attitude", example: "A positive attitude makes learning sustainable.", mnemonic: "attitude 里有 altitude 高度：态度决定高度。", category: "人物品质" },
  { word: "capacity", meaning: "能力；容量", phrase: "learning capacity", example: "Reading can increase your capacity for understanding.", mnemonic: "cap 装住：能装多少就是容量/能力。", category: "学术阅读" },
  { word: "circumstance", meaning: "情况；环境", phrase: "under no circumstances", example: "Under no circumstances should we give up.", mnemonic: "circum 圆周：围绕人的情况。", category: "高考核心" },
  { word: "combine", meaning: "结合", phrase: "combine theory with practice", example: "Combine word cards with reading practice.", mnemonic: "com 一起 + bine 绑：绑在一起。", category: "写作表达" },
  { word: "complex", meaning: "复杂的", phrase: "a complex problem", example: "Break complex sentences into small parts.", mnemonic: "com + plex 编织：编在一起就复杂。", category: "阅读题型" },
  { word: "conduct", meaning: "实施；行为", phrase: "conduct a survey", example: "The students conducted a survey on reading habits.", mnemonic: "con + duct 引导：引导一件事发生。", category: "学术阅读" },
  { word: "confidence", meaning: "信心", phrase: "build confidence", example: "Small wins build confidence.", mnemonic: "confide 是信任，confidence 是对自己的信任。", category: "人物品质" },
  { word: "confirm", meaning: "确认；证实", phrase: "confirm the information", example: "Confirm the answer with evidence from the text.", mnemonic: "firm 坚定：让信息变坚实。", category: "阅读题型" },
  { word: "conflict", meaning: "冲突；矛盾", phrase: "solve a conflict", example: "The story shows a conflict between duty and desire.", mnemonic: "con 一起 + flict 打击：撞在一起。", category: "文学阅读" },
  { word: "consider", meaning: "考虑；认为", phrase: "consider doing", example: "Consider reviewing mistakes before new exercises.", mnemonic: "sid 星：古人看星思考，consider 就是考虑。", category: "高考核心" },
  { word: "creativity", meaning: "创造力", phrase: "develop creativity", example: "Art and science both need creativity.", mnemonic: "create 创造 + ity 名词：创造的能力。", category: "写作表达" },
  { word: "decline", meaning: "下降；拒绝", phrase: "a sharp decline", example: "His scores declined because he stopped reviewing.", mnemonic: "de 向下 + cline 斜坡：沿坡下降。", category: "图表写作" },
  { word: "define", meaning: "定义；界定", phrase: "define success", example: "Different people define success differently.", mnemonic: "fine 边界：划边界就是定义。", category: "学术阅读" },
  { word: "demand", meaning: "要求；需求", phrase: "meet the demand", example: "Modern society demands lifelong learning.", mnemonic: "mand 命令：强烈要求。", category: "高考核心" },
  { word: "desire", meaning: "渴望；愿望", phrase: "a desire to learn", example: "A strong desire can drive action.", mnemonic: "de + sire 像“点燃”：愿望像火。", category: "人物品质" },
  { word: "emphasize", meaning: "强调", phrase: "emphasize the importance", example: "Teachers emphasize the importance of review.", mnemonic: "em + phase 阶段：把某一部分凸显出来。", category: "写作表达" },
  { word: "enable", meaning: "使能够", phrase: "enable sb. to do", example: "Vocabulary enables us to understand more texts.", mnemonic: "en 使 + able 能：使能做。", category: "写作表达" },
  { word: "encourage", meaning: "鼓励", phrase: "encourage sb. to do", example: "Parents should encourage children to read.", mnemonic: "en + courage 勇气：给勇气。", category: "校园生活" },
  { word: "essential", meaning: "必要的；本质的", phrase: "be essential for", example: "Daily practice is essential for language learning.", mnemonic: "essence 本质：本质需要的就是必要的。", category: "写作表达" },
  { word: "establish", meaning: "建立；确立", phrase: "establish a habit", example: "Establish a fixed review routine.", mnemonic: "stable 稳定：建立后变稳定。", category: "学习方法" },
  { word: "financial", meaning: "金融的；财务的", phrase: "financial support", example: "Financial education helps students make wise choices.", mnemonic: "finance 财务 + ial 形容词。", category: "社会生活" },
  { word: "ignore", meaning: "忽视", phrase: "ignore mistakes", example: "Never ignore repeated spelling mistakes.", mnemonic: "ignorant 无知：装不知道就是忽视。", category: "易错诊断" },
  { word: "illustrate", meaning: "说明；阐明", phrase: "illustrate the idea", example: "Examples can illustrate abstract ideas.", mnemonic: "illuminate 照亮：把意思照亮。", category: "学术阅读" },
  { word: "independent", meaning: "独立的", phrase: "independent thinking", example: "Students need independent thinking.", mnemonic: "in 不 + depend 依赖：不依赖。", category: "人物品质" },
  { word: "influence", meaning: "影响", phrase: "have an influence on", example: "Friends have a strong influence on teenagers.", mnemonic: "in + flu 流入：流进去产生影响。", category: "写作表达" },
  { word: "inspire", meaning: "激励；启发", phrase: "inspire students", example: "Good stories inspire us to act.", mnemonic: "spirit 精神：把精神吹进去。", category: "人物品质" },
  { word: "issue", meaning: "问题；发行", phrase: "a social issue", example: "Environmental protection is a global issue.", mnemonic: "issue 像“议题”：拿出来讨论的问题。", category: "社会热点" },
  { word: "majority", meaning: "大多数", phrase: "the majority of", example: "The majority of students prefer visual notes.", mnemonic: "major 主要的：主要部分就是大多数。", category: "图表写作" },
  { word: "measure", meaning: "措施；测量", phrase: "take measures", example: "We should take measures to protect nature.", mnemonic: "measure 可量也可“措施”，都是为解决问题定尺度。", category: "写作表达" },
  { word: "mental", meaning: "心理的；精神的", phrase: "mental health", example: "Mental health is as important as grades.", mnemonic: "mind 心智 → mental 心理的。", category: "社会生活" },
  { word: "occur", meaning: "发生；出现", phrase: "occur to sb.", example: "A good idea occurred to me during reading.", mnemonic: "cur 跑：事情跑到眼前就发生。", category: "高考核心" },
  { word: "opportunity", meaning: "机会", phrase: "seize an opportunity", example: "Every mistake is an opportunity to improve.", mnemonic: "port 港口：机会像通向外面的港口。", category: "写作表达" },
  { word: "particular", meaning: "特别的；具体的", phrase: "in particular", example: "This method is useful for reading in particular.", mnemonic: "particle 小颗粒：具体到某一粒。", category: "高考核心" },
  { word: "process", meaning: "过程；处理", phrase: "in the process of", example: "Learning is a process, not a one-day task.", mnemonic: "pro 向前 + cess 走：向前走的过程。", category: "学习方法" },
  { word: "recommend", meaning: "推荐；建议", phrase: "recommend doing", example: "I recommend reading aloud every morning.", mnemonic: "re 再 + commend 称赞：反复称赞所以推荐。", category: "写作表达" },
  { word: "reduce", meaning: "减少", phrase: "reduce stress", example: "A clear plan reduces stress.", mnemonic: "re 向后 + duce 引导：把数量引回去。", category: "写作表达" },
  { word: "reflect", meaning: "反映；反思", phrase: "reflect on mistakes", example: "Reflect on your mistakes after each test.", mnemonic: "re 回 + flect 弯：把思考弯回自己身上。", category: "学习方法" },
  { word: "relevant", meaning: "相关的", phrase: "be relevant to", example: "Choose evidence relevant to the question.", mnemonic: "relate 关联：相关的。", category: "阅读题型" },
  { word: "represent", meaning: "代表；象征", phrase: "represent a culture", example: "The dragon represents Chinese culture.", mnemonic: "re + present 再呈现：替某物呈现。", category: "文化阅读" },
  { word: "require", meaning: "需要；要求", phrase: "require patience", example: "Mastering English requires patience.", mnemonic: "re + quire 寻求：反复寻求就是需要。", category: "高考核心" },
  { word: "responsibility", meaning: "责任", phrase: "take responsibility", example: "Students should take responsibility for their choices.", mnemonic: "response 回应：该回应的事就是责任。", category: "人物品质" },
  { word: "solution", meaning: "解决办法", phrase: "find a solution", example: "A checklist is a simple solution to careless mistakes.", mnemonic: "solve 解决 + tion 名词。", category: "写作表达" },
  { word: "specific", meaning: "具体的；特定的", phrase: "specific details", example: "Use specific details to support your point.", mnemonic: "spec 看：看得很具体。", category: "写作表达" },
  { word: "sustainable", meaning: "可持续的", phrase: "sustainable development", example: "Sustainable habits matter more than short bursts.", mnemonic: "sustain 维持 + able：能维持下去。", category: "社会热点" },
  { word: "unique", meaning: "独特的", phrase: "a unique experience", example: "Everyone has a unique way of learning.", mnemonic: "uni 一个：只有一个，所以独特。", category: "写作表达" },
];

const ENGLISH_WORD_BANK =
  Array.isArray(window.ENGLISH_WORD_BANK) && window.ENGLISH_WORD_BANK.length
    ? window.ENGLISH_WORD_BANK
    : ENGLISH_WORD_BANK_FALLBACK;

const wordTrainerState = {
  index: 0,
  category: "全部",
  feedback: "",
  exampleFeedback: "",
  examplePassed: false,
  quizCount: 20,
  sessionKeys: [],
  sessionTitle: "",
  step: 0,
};
const WORD_TRAINER_STEPS = ["意思辨认", "词汇复述", "例句填空", "助记复述", "拼写确认"];

const LEARNING_METHODS = [
  {
    title: "知识学习：闭环式学习",
    tag: "拒绝死记硬背 · 吃透、锁牢、会用",
    summary: "把“听懂了”变成“讲得出、用得上”。每一节课按预习、课堂、复盘三步闭环，减少知识漏洞。",
    steps: [
      {
        name: "轻量预习",
        time: "课前 15–30 分钟",
        action: "只做预习三问：这节课学什么？哪些一看就懂？哪些完全不懂？",
        tips: ["圈课本关键词和疑难点", "不啃难题、不背全文", "带着问题进课堂，专注找答案"],
      },
      {
        name: "专注课堂",
        time: "45 分钟主战场",
        action: "听老师的逻辑和方法，不追求把笔记抄满；当堂问题当堂标记、当堂解决。",
        tips: ["优先听知识框架、解题方法、易错点", "老师反复强调处用星号标记", "课上没听懂，课后立刻问，不让漏洞过夜"],
      },
      {
        name: "费曼复盘",
        time: "课后 5–10 分钟",
        action: "合上书，用大白话把知识点讲给零基础同学听；讲不顺的地方就是盲区。",
        tips: ["必须讲原理、用法、易错点", "卡顿处回书本/笔记补洞", "一章结束后再讲一次完整主线"],
      },
    ],
    checklist: ["我今天是否带着问题听课？", "今天有没有一个疑问当堂清零？", "我能否不用术语讲清一个新知识点？"],
  },
  {
    title: "刷题提分：精准高效刷题",
    tag: "学一道 · 会一类 · 通一片",
    summary: "刷题不拼数量，拼选题质量和复盘深度。题海战术换成核心题型、错题复盘和知识体系。",
    steps: [
      {
        name: "精准选题",
        time: "每天先选再刷",
        action: "只抓三类题：课本例题/课后经典题、作业高频错题、考试高频考点题。",
        tips: ["基础弱先刷基础题", "基础稳再刷中档题和变式题", "偏题怪题超纲题先放弃"],
      },
      {
        name: "错题复盘",
        time: "做题时间的一半",
        action: "错题本不抄题海，写清考点、错因、标准套路和避坑提醒。",
        tips: ["错因分成知识不会、思路不对、审题粗心、计算失误", "当天整理，隔天翻看，周末集中复盘", "同类题再找 1–2 道验证是否真会"],
      },
      {
        name: "知识体系",
        time: "每周 20–30 分钟",
        action: "用一张简图串联本周知识点、考点、题型、易错点。",
        tips: ["不追求漂亮，追求关系清楚", "把错题挂到对应知识点下", "考试前按图快速调取方法"],
      },
    ],
    checklist: ["我今天刷的是核心题型吗？", "每道错题是否写了真实错因？", "本周知识点是否已经连成一张图？"],
  },
  {
    title: "时间管理：规律换高效",
    tag: "不靠熬夜内卷 · 只算有效专注时长",
    summary: "稳定作息、碎片时间和番茄钟，解决“坐很久但没学进去”的假努力。",
    steps: [
      {
        name: "三段式节奏",
        time: "每天固定",
        action: "早读背记忆类，晚间攻数理化和深度题，睡前 10 分钟复盘当天漏洞和明日任务。",
        tips: ["周末假期只微调，不极端摆烂也不爆学", "让身体形成学习惯性", "睡前只复盘和计划，不再硬刷难题"],
      },
      {
        name: "碎片时间",
        time: "课间/自习/午休空档",
        action: "在校优先完成基础作业，把晚上留给复盘、刷题、补短板。",
        tips: ["课间可背 3 个单词/1 个公式/1 首古诗关键句", "自习课先清书面作业", "晚上不再被作业追着跑"],
      },
      {
        name: "番茄钟专注法",
        time: "25 + 5 分钟",
        action: "25 分钟只做一件事，不碰手机；5 分钟休息眼睛和身体，再开下一轮。",
        tips: ["每轮只设一个小目标", "手机离开桌面", "休息时站起来、远眺、喝水，不刷短视频"],
      },
    ],
    checklist: ["今天是否有一段完整 25 分钟无干扰学习？", "基础作业是否尽量在校完成？", "睡前是否做了 10 分钟复盘？"],
  },
  {
    title: "身心调节：稳住状态",
    tag: "长期提分的根基 · 少内耗，多行动",
    summary: "成绩起伏常常不是智商问题，而是焦虑、透支和拖延。把状态稳住，学习才能长期有效。",
    steps: [
      {
        name: "接纳不完美",
        time: "每次考试后",
        action: "考试不是审判，而是体检；只和昨天的自己比，找到漏洞就是收获。",
        tips: ["把排名焦虑改成漏洞清单", "允许犯错，但不允许同错反复犯", "每天只追一个小进步"],
      },
      {
        name: "劳逸结合",
        time: "每天都要有",
        action: "用运动和短休息维护专注力，别用熬夜透支第二天课堂。",
        tips: ["跑步、跳绳、拉伸都可以", "学习累了远眺 1–2 分钟", "困到低效时先休息，不硬撑假学"],
      },
      {
        name: "拒绝拖延",
        time: "从 10 分钟开始",
        action: "不想学时先做 10 分钟；难题先拆第一步，一旦开始就会进入状态。",
        tips: ["任务拆到小到不能再小", "完成后打勾积累成就感", "薄弱点每天碰一点，不让问题堆成山"],
      },
    ],
    checklist: ["今天有没有把一次焦虑转成具体行动？", "今天是否运动或拉伸过？", "拖延任务是否至少启动了 10 分钟？"],
  },
];

const DAILY_METHOD_FLOW = [
  ["课前", "15–30 分钟轻量预习：标重点、圈疑问、带问题进课堂"],
  ["课堂", "听逻辑、抓方法、记易错点；疑问当堂标记，当天清零"],
  ["课后", "用费曼法讲一遍，讲不顺就回书本补洞"],
  ["刷题", "只刷核心题型；错题写考点、错因、套路、避坑"],
  ["睡前", "10 分钟复盘漏洞，安排明天最重要的 1–3 件事"],
];

const WRONG_QUESTION_TEMPLATE = [
  "考点：这题到底考哪个知识点？",
  "错因：知识不会 / 思路不对 / 审题粗心 / 计算失误 / 时间分配不当",
  "标准套路：正确第一步是什么？关键公式、模型或答题结构是什么？",
  "避坑提醒：下次看到什么信号要警惕？",
  "复盘节奏：当天整理，隔天翻看，周末集中复盘并找同类题验证",
];

const BILIBILI_VIDEO_RESOURCES = [
  {
    subject: "语文",
    curator: "学过石油的语文老师",
    role: "基础预习 / 应试提分主推",
    homepage: biliUpSearchUrl("学过石油的语文老师"),
    collection: biliSearchUrl("学过石油的语文老师"),
    why: "语文先抓踩分点：文言文、现代文阅读和作文模板要能落到答题纸上；基础弱先用应试向资源快速建立题型意识。",
    watchOrder: ["文言文实词虚词与翻译", "现代文阅读题型", "作文审题立意", "古诗文鉴赏与素材积累"],
    keywords: ["文言文", "现代文阅读", "作文模板", "古诗词", "文学素养"],
    note: "基础弱先看应试拆解，瓶颈期再补文本深读和文学素养。",
    recommendations: [
      {
        name: "学过石油的语文老师",
        level: "基础 / 预习 / 提分",
        focus: "文言文、现代文阅读、作文答题模板",
        fit: "基础弱、初高衔接、高一高二同步预习和快速抓踩分点。",
        summary: "接地气应试向，适配新教材，阅读和作文拆解完整，容易照着做。",
      },
      {
        name: "国家玮",
        level: "拔高 / 文学思维",
        focus: "文本深度解读、大阅读难题、语文格局",
        fit: "语文瓶颈、高三二轮、想提升文本理解深度的同学。",
        summary: "重文学思维和文本纵深，适合从“会答题”升级到“读得深”。",
      },
      {
        name: "中文系五匪子",
        level: "作文专项",
        focus: "议论文立意、结构模板、高考阅卷标准",
        fit: "作文跑题、无话可写、结构混乱的同学。",
        summary: "专攻议论文写作，直接对标高考作文评分和结构表达。",
      },
      {
        name: "高盛元",
        level: "古诗文素养",
        focus: "唐诗宋词、诗词鉴赏、古诗文底蕴",
        fit: "古诗文鉴赏失分多、语感薄弱、需要碎片化积累的同学。",
        summary: "适合日常补诗词语感和文学背景，提升鉴赏题底层素材。",
      },
    ],
  },
  {
    subject: "数学（人教A版）",
    curator: "一数",
    role: "高中数学系统课 / 压轴题思路主推",
    homepage: "https://space.bilibili.com/14229967",
    collection: biliSearchUrl("一数"),
    why: "数学先补体系，再做模型归类。基础薄弱先跟一数完整过课本，中等生再用专题课突破解析几何、导数和压轴题。",
    watchOrder: ["集合与函数基础", "三角函数与向量", "解析几何与圆锥曲线", "数列与导数综合"],
    keywords: ["函数", "导数", "圆锥曲线", "数列", "压轴题"],
    note: "零基础用系统课，中高分用专题模型和压轴题归类。",
    recommendations: [
      {
        name: "一数",
        level: "零基础 / 补基础",
        focus: "初高衔接、必修、选择性必修、一轮二轮复习",
        fit: "基础差、步骤容易断、需要完整免费体系的同学。",
        summary: "板书细致，步骤无跳跃，错题易错点反复强调，适合作为主线课。",
      },
      {
        name: "佟大大还是ETT",
        level: "拔高 / 压轴",
        focus: "解析几何、导数、立体几何模型技巧",
        fit: "中等生突破压轴题、想提升高分段稳定性的同学。",
        summary: "题型归类强，适合把复杂题拆成模型和套路。",
      },
      {
        name: "赵显礼（显哥数学）",
        level: "高三应试",
        focus: "题型总结、押题思路、高考解题路径",
        fit: "高三系统刷题复盘、需要高密度考点整理的同学。",
        summary: "老牌应试名师，知识点密度高，适合总复习阶段提效率。",
      },
      {
        name: "数学微课帮",
        level: "同步 / 查漏补缺",
        focus: "单章节专题、校内同步巩固",
        fit: "某一章听不扎实、需要短视频补洞的高一高二学生。",
        summary: "模块化讲解，适合用来补单点漏洞，不必从头刷整套课。",
      },
    ],
  },
  {
    subject: "英语",
    curator: "英语兔",
    role: "语法体系 / 长难句主推",
    homepage: "https://search.bilibili.com/upuser?keyword=%E8%8B%B1%E8%AF%AD%E5%85%94",
    collection: biliSearchUrl("英语兔"),
    why: "英语先搭语法和长难句底层框架，再专项训练阅读、完形、七选五和作文。听力口语可用外教资源做长期语感积累。",
    watchOrder: ["句子成分", "时态语态", "三大从句", "非谓语动词", "长难句拆解"],
    keywords: ["语法", "长难句", "从句", "非谓语", "阅读理解"],
    note: "语法框架要回到阅读长句中验证，应试题型要单独训练。",
    recommendations: [
      {
        name: "英语兔",
        level: "基础 / 语法入门",
        focus: "语法、音标、词根词缀、长难句底层逻辑",
        fit: "0基础、语法碎片化、看不懂长难句的同学。",
        summary: "动画可视化讲解，逻辑简洁，适合从头搭英语框架。",
      },
      {
        name: "Free高考英语",
        level: "拔高 / 阅读题型",
        focus: "长难句拆解、阅读逻辑、七选五、完形",
        fit: "想精细化抠阅读题型、冲英语高分的同学。",
        summary: "题型拆解细，适合高分段精修阅读和逻辑链。",
      },
      {
        name: "李辉英语",
        level: "高三应试 / 短期提分",
        focus: "完形、阅读套路、作文万能句式",
        fit: "高三冲刺、需要快速形成应试套路的同学。",
        summary: "提分导向强，适合考前把题型和作文表达标准化。",
      },
      {
        name: "瑞秋Rachel",
        level: "听口 / 语感",
        focus: "美式发音、听力口语、磨耳朵",
        fit: "发音不准、听力弱、想长期积累语感的同学。",
        summary: "外教发音资源，适合作为课外听口训练补充。",
      },
    ],
  },
  {
    subject: "物理",
    curator: "黄夫人HuangFuRen",
    role: "高一入门 / 基础救星",
    homepage: biliUpSearchUrl("黄夫人HuangFuRen"),
    collection: biliSearchUrl("黄夫人HuangFuRen"),
    why: "物理要先把抽象模型生活化：受力分析、运动学、电磁学先听懂图像和过程，再进入综合题模型训练。",
    watchOrder: ["运动和力", "能量与动量", "电场与电路", "磁场与电磁感应", "近代物理拓展"],
    keywords: ["力学", "电磁学", "能量", "动量", "物理模型"],
    note: "高一听不懂先补黄夫人，中高分再上模型和大题专项。",
    recommendations: [
      {
        name: "黄夫人HuangFuRen",
        level: "基础 / 高一入门",
        focus: "受力分析、运动学、电磁学生活化拆解",
        fit: "刚上高中物理听不懂、力学模型混乱的同学。",
        summary: "幽默通俗，力学板块口碑强，适合从“听不懂”救回来。",
      },
      {
        name: "夏梦迪",
        level: "拔高 / 中高分",
        focus: "定理本质、模型、图像化、电磁和动量能量综合",
        fit: "有基础但卡在电场磁场、动量能量综合大题的同学。",
        summary: "重模型和本质，适合从会做基础题提升到会解综合题。",
      },
      {
        name: "于总CEO",
        level: "预习 / 整册框架",
        focus: "整本书知识框架、知识点拆解",
        fit: "想预习整册、梳理知识脉络、防止碎片化的同学。",
        summary: "先搭大框架再拆知识点，适合开学前和章节前预习。",
      },
      {
        name: "坤哥物理",
        level: "专项 / 应试",
        focus: "电学、实验题、计算大题技巧",
        fit: "中等生专项突破电学、实验、计算类大题。",
        summary: "题型套路清晰，适合刷题后做专项归纳。",
      },
    ],
  },
  {
    subject: "化学",
    curator: "李政化学",
    role: "高三应试 / 二轮冲刺主推",
    homepage: biliUpSearchUrl("李政化学"),
    collection: biliSearchUrl("李政化学"),
    why: "化学既要课本基础，也要大题方法论。高一高二先同步打基础，高三再上工业流程、有机、电化学和实验题套路。",
    watchOrder: ["实验现象观察", "微观结构解释", "反应原理迁移", "生活化学与材料"],
    keywords: ["化学反应", "实验", "结构性质", "反应原理", "科普"],
    note: "同步基础、体系复盘、二轮冲刺和兴趣实验要分开选课。",
    recommendations: [
      {
        name: "李政化学",
        level: "高三应试 / 冲刺",
        focus: "工业流程、有机化学、电化学、实验大题",
        fit: "高三二轮、考前突击、想快速掌握大题方法论的同学。",
        summary: "口诀和解题方法论密集，考点深挖，冲刺提分强。",
      },
      {
        name: "一化儿",
        level: "零基础 / 同步课本",
        focus: "课本逐课精讲、方程式记忆、基础知识拆解",
        fit: "高一高二同步上课、补基础短板的同学。",
        summary: "讲解简单易懂，适合先把课本和方程式过扎实。",
      },
      {
        name: "化学超人",
        level: "一轮体系 / 刷题",
        focus: "一轮复习、PPT讲义、考点分类刷题",
        fit: "自主一轮系统复盘、想按考点分类刷题的同学。",
        summary: "体系完整，适合把零散知识整理成一轮复习框架。",
      },
      {
        name: "真·凤舞九天",
        level: "兴趣 / 实验理解",
        focus: "趣味化学实验、反应现象、原理直观化",
        fit: "想培养兴趣、理解抽象反应过程的同学。",
        summary: "用实验现象帮助理解化学反应，不替代考试体系。",
      },
    ],
  },
  {
    subject: "生物",
    curator: "汉水丑生侯伟",
    role: "课本知识点 / 基础体系主推",
    homepage: biliUpSearchUrl("汉水丑生侯伟"),
    collection: biliSearchUrl("汉水丑生侯伟"),
    why: "生物先回到教材原文、图表和概念边界，再用题目绑定考点。基础薄弱先逐句梳理课本，高三再训练话术和主观题表达。",
    watchOrder: ["生物分类与鉴定", "生态关系", "进化适应", "人与自然专题"],
    keywords: ["生态", "进化", "分类", "生命科学", "生物多样性"],
    note: "教材是生物提分核心，考前再做高频考点和话术压缩。",
    recommendations: [
      {
        name: "汉水丑生侯伟",
        level: "基础 / 课本体系",
        focus: "教材逐句梳理、图文思维导图、必修与选择性必修",
        fit: "需要扎实过课本、概念边界总混的同学。",
        summary: "课本知识点天花板，很多老师备课也会参考，打基础优先看。",
      },
      {
        name: "李林生物（德叔）",
        level: "一轮 / 边学边练",
        focus: "知识点 + 《李林1000题》刷题讲解",
        fit: "高三一轮、想学完马上练题巩固的同学。",
        summary: "风趣易懂，知识点和习题绑定，适合复习闭环。",
      },
      {
        name: "万猛生物",
        level: "考前 / 话术",
        focus: "高频考点、主观题答题话术、快速背诵复盘",
        fit: "考前冲刺、主观题表达不规范的同学。",
        summary: "适合压缩高频考点，抓规范得分话术。",
      },
    ],
  },
  {
    subject: "历史",
    curator: "历史老师定哥",
    role: "体系速通 / 答题模板主推",
    homepage: biliUpSearchUrl("历史老师定哥"),
    collection: biliSearchUrl("历史老师定哥"),
    why: "历史要先有时间线和通史框架，再补因果逻辑和材料题答题模板。体系薄弱先搭框架，兴趣资源用于预习和减压。",
    watchOrder: ["世界近现代人物", "国家制度与变迁", "战争与国际秩序", "材料题背景迁移"],
    keywords: ["世界史", "人物史", "国际关系", "制度变迁", "历史背景"],
    note: "看完视频要回到教材时间线和史料题设问里整理。",
    recommendations: [
      {
        name: "历史老师定哥",
        level: "体系 / 应试",
        focus: "思维导图、时间线、答题模板、《13天通关高中历史》",
        fit: "缺通史框架、想快速搭建历史体系的同学。",
        summary: "干货密集，适合速通中外通史；语速偏快可倍速/降速观看。",
      },
      {
        name: "讲历史的张老师",
        level: "理解 / 专题",
        focus: "朝代模块、专题模块、事件因果逻辑",
        fit: "不想死记时间线、想理解历史背景和因果的同学。",
        summary: "逻辑梳理清晰，适合理解事件为什么发生、怎样发展。",
      },
      {
        name: "段sir是懂王",
        level: "兴趣 / 预习",
        focus: "趣味化历史事件、预习、缓解背诵压力",
        fit: "历史兴趣弱、背诵压力大、需要先建立画面感的同学。",
        summary: "适合预习和兴趣导入，再回到课本做体系化记忆。",
      },
    ],
  },
  {
    subject: "地理",
    curator: "安迎老师",
    role: "地理原理 / 老牌名师主推",
    homepage: biliUpSearchUrl("安迎老师"),
    collection: biliSearchUrl("安迎老师"),
    why: "地理要从底层原理和图像题入手：等值线、气候、地貌、区域地理先吃透，再用真题模板标准化表达。",
    watchOrder: ["地貌与水文", "气候与植被", "中国区域地理", "资源环境与国家安全"],
    keywords: ["自然地理", "区域地理", "地图", "气候", "人地关系"],
    note: "自然地理重原理，人文地理重模板，区域地理要配合地图册。",
    recommendations: [
      {
        name: "安迎老师",
        level: "基础原理 / 系统",
        focus: "课本核心原理、等值线、区域地理",
        fit: "想吃透地理底层逻辑、摆脱死记硬背的同学。",
        summary: "逻辑严谨，老牌名师，适合把自然地理原理讲透。",
      },
      {
        name: "张艳平地理",
        level: "高考应试 / 大题",
        focus: "高考真题、大题模板、自然地理和人文地理答题思路",
        fit: "高三提分、材料题和综合题表达不规范的同学。",
        summary: "真题拆解和模板化表达强，应试针对性高。",
      },
      {
        name: "羊羊的地理教室",
        level: "同步 / 查漏补缺",
        focus: "高一高二同步、细碎知识点归纳",
        fit: "零碎知识点容易漏、需要同步巩固的同学。",
        summary: "适合补小知识点和日常同步，不必等到总复习才看。",
      },
    ],
  },
  {
    subject: "思想政治",
    curator: "刘勖雯",
    role: "政治应试 / 主观题模板主推",
    homepage: biliUpSearchUrl("刘勖雯"),
    collection: biliSearchUrl("刘勖雯"),
    why: "政治要先过教材原文，再把选择题排除和主观题分题型模板练熟；高三后期要结合年度时政热点做材料题。",
    watchOrder: ["法治案例", "权利义务", "社会热点", "青年责任与价值表达"],
    keywords: ["法治", "法律生活", "权利义务", "时政", "公共参与"],
    note: "政治必须用教材术语作答，模板和时政都要服务于材料分析。",
    recommendations: [
      {
        name: "刘勖雯",
        level: "应试 / 模板",
        focus: "选择题排除技巧、主观题万能模板、新教材必修梳理",
        fit: "主观题没话说、选择题总纠结、需要直接套用话术的同学。",
        summary: "应试性强，适合把题型和答题语言标准化。",
      },
      {
        name: "朱法垚",
        level: "时政 / 高三后期",
        focus: "年度时政热点、材料分析大题",
        fit: "高三后期时政专题复习、材料题结合热点困难的同学。",
        summary: "擅长把时政热点拆成考题逻辑和答题角度。",
      },
      {
        name: "咕噜咕噜将",
        level: "基础 / 教材串讲",
        focus: "教材原文逐课串讲、知识框架",
        fit: "基础薄弱、需要先把课本知识点过一遍的同学。",
        summary: "框架清晰，适合先打底，再进入模板和热点训练。",
      },
    ],
  },
];

const $ = (selector) => document.querySelector(selector);
const subjectList = $("#subjectList");
const volumeTabs = $("#volumeTabs");
const unitList = $("#unitList");
const breadcrumb = $("#breadcrumb");
const stats = $("#stats");
const subjectCount = $("#subjectCount");
const searchInput = $("#searchInput");
const resetBtn = $("#resetBtn");
const modeSwitch = $("#modeSwitch");

let doneSet = readProgress();
const KNOWLEDGE_NODE_TOTAL = countAllKnowledgeNodes();

function readProgress() {
  try {
    return new Set(JSON.parse(localStorage.getItem(STORAGE_KEY) || "[]"));
  } catch {
    return new Set();
  }
}

function saveProgress() {
  localStorage.setItem(STORAGE_KEY, JSON.stringify([...doneSet]));
}

function hidePageLoader() {
  const loader = document.querySelector("#pageLoader");
  if (!loader) return;
  loader.classList.add("is-hidden");
  window.setTimeout(() => {
    loader.remove();
  }, 500);
}

function courseTotals() {
  const volumes = COURSE.reduce((sum, subject) => sum + subject.volumes.length, 0);
  const units = COURSE.reduce(
    (sum, subject) => sum + subject.volumes.reduce((inner, volume) => inner + volume.units.length, 0),
    0,
  );
  return { subjects: COURSE.length, volumes, units, done: doneSet.size, knowledgeNodes: KNOWLEDGE_NODE_TOTAL };
}

function keyFor(subjectIndex, volumeIndex, unitIndex) {
  return `${subjectIndex}-${volumeIndex}-${unitIndex}`;
}

function escapeHTML(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

function escapeRegExp(value) {
  return String(value).replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

function highlight(text) {
  const safeText = escapeHTML(text);
  const query = state.query.trim();
  if (!query) return safeText;
  const safeQuery = escapeHTML(query);
  return safeText.replace(new RegExp(escapeRegExp(safeQuery), "gi"), (match) => `<mark class="mark">${match}</mark>`);
}

function containsQuery(...fields) {
  const q = state.query.trim().toLowerCase();
  if (!q) return true;
  return fields.some((field) => String(field).toLowerCase().includes(q));
}

function readWordTrainerProgress() {
  try {
    const raw = JSON.parse(localStorage.getItem(WORD_TRAINER_STORAGE_KEY) || "{}");
    return raw && typeof raw === "object" ? raw : {};
  } catch {
    return {};
  }
}

function saveWordTrainerProgress(progress) {
  localStorage.setItem(WORD_TRAINER_STORAGE_KEY, JSON.stringify(progress));
}

function wordKey(item) {
  return item.word.toLowerCase();
}

function wordWrongCount(item, progress = readWordTrainerProgress()) {
  return progress[wordKey(item)]?.wrong || 0;
}

function wordHasWrongRecord(item, progress = readWordTrainerProgress()) {
  return wordWrongCount(item, progress) > 0;
}

function wordCategories() {
  return ["全部", "优先复习", "错题回顾", ...Array.from(new Set(ENGLISH_WORD_BANK.map((item) => item.category)))];
}

function wordCategoryCount(category) {
  if (category === "全部") return ENGLISH_WORD_BANK.length;
  if (category === "优先复习") {
    const progress = readWordTrainerProgress();
    return ENGLISH_WORD_BANK.filter((item) => wordProgressLevel(item, progress) < 2).length;
  }
  if (category === "错题回顾") {
    const progress = readWordTrainerProgress();
    return ENGLISH_WORD_BANK.filter((item) => wordHasWrongRecord(item, progress)).length;
  }
  return ENGLISH_WORD_BANK.filter((item) => item.category === category).length;
}

function wordMatchesQuery(item) {
  return containsQuery(item.word, item.phonetic, item.meaning, item.phrase, item.example, item.mnemonic, item.category);
}

function baseWordTrainerWords() {
  const progress = readWordTrainerProgress();
  return ENGLISH_WORD_BANK.filter(
    (item) =>
      (wordTrainerState.category === "全部" ||
        (wordTrainerState.category === "优先复习"
          ? wordProgressLevel(item, progress) < 2
          : wordTrainerState.category === "错题回顾"
            ? wordHasWrongRecord(item, progress)
          : item.category === wordTrainerState.category)) &&
      wordMatchesQuery(item),
  ).sort((left, right) =>
    wordTrainerState.category === "优先复习"
      ? wordProgressLevel(left, progress) - wordProgressLevel(right, progress)
      : wordTrainerState.category === "错题回顾"
        ? wordWrongCount(right, progress) - wordWrongCount(left, progress)
        : 0,
  );
}

function wordTrainerWords() {
  if (!wordTrainerState.sessionKeys.length) return baseWordTrainerWords();
  const byKey = new Map(ENGLISH_WORD_BANK.map((item) => [wordKey(item), item]));
  return wordTrainerState.sessionKeys.map((key) => byKey.get(key)).filter(Boolean);
}

function currentWordList() {
  const words = wordTrainerWords();
  if (!words.length) return words;
  if (wordTrainerState.index >= words.length) wordTrainerState.index = 0;
  if (wordTrainerState.index < 0) wordTrainerState.index = words.length - 1;
  return words;
}

function wordProgressLevel(item, progress = readWordTrainerProgress()) {
  return progress[wordKey(item)]?.level || 0;
}

function wordLevelLabel(level) {
  if (level >= 2) return "已掌握";
  if (level === 1) return "模糊";
  return "未掌握";
}

function wordLevelClass(level) {
  if (level >= 2) return "is-known";
  if (level === 1) return "is-fuzzy";
  return "is-new";
}

function wordProgressStats(words, progress) {
  return words.reduce(
    (stats, item) => {
      const level = wordProgressLevel(item, progress);
      if (level >= 2) stats.known += 1;
      else if (level === 1) stats.fuzzy += 1;
      else stats.fresh += 1;
      if (wordHasWrongRecord(item, progress)) stats.wrong += 1;
      return stats;
    },
    { known: 0, fuzzy: 0, fresh: 0, wrong: 0 },
  );
}

function wordQuizOptions(current) {
  const distractors = ENGLISH_WORD_BANK.filter((item) => item.word !== current.word).map((item) => item.meaning);
  const seed = current.word.split("").reduce((sum, char) => sum + char.charCodeAt(0), 0);
  const picked = [];
  for (let offset = 0; picked.length < 3 && offset < distractors.length; offset += 1) {
    const meaning = distractors[(seed + offset * 7) % distractors.length];
    if (!picked.includes(meaning)) picked.push(meaning);
  }
  const options = [...picked];
  options.splice(seed % (options.length + 1), 0, current.meaning);
  return options;
}

function readWordQuizCount() {
  const input = document.querySelector("#wordQuizCount");
  const raw = Number.parseInt(input?.value || wordTrainerState.quizCount || 20, 10);
  const count = Number.isFinite(raw) ? raw : 20;
  wordTrainerState.quizCount = Math.min(200, Math.max(1, count));
  return wordTrainerState.quizCount;
}

function shuffledSample(words, count) {
  const pool = [...words];
  for (let index = pool.length - 1; index > 0; index -= 1) {
    const swapIndex = Math.floor(Math.random() * (index + 1));
    [pool[index], pool[swapIndex]] = [pool[swapIndex], pool[index]];
  }
  return pool.slice(0, Math.min(count, pool.length));
}

function resetWordTrainerTransientState() {
  wordTrainerState.feedback = "";
  wordTrainerState.exampleFeedback = "";
  wordTrainerState.examplePassed = false;
  wordTrainerState.step = 0;
}

function startWordQuizSession(words, title) {
  if (!words.length) {
    wordTrainerState.feedback = "当前范围没有可抽取的单词。可以先切换到“全部”，或清空搜索条件。";
    return;
  }
  wordTrainerState.sessionKeys = words.map((item) => wordKey(item));
  wordTrainerState.sessionTitle = title;
  wordTrainerState.index = 0;
  resetWordTrainerTransientState();
  wordTrainerState.feedback = `已开始：${title}，共 ${words.length} 个词。`;
}

function endWordQuizSession() {
  wordTrainerState.sessionKeys = [];
  wordTrainerState.sessionTitle = "";
  wordTrainerState.index = 0;
  resetWordTrainerTransientState();
  wordTrainerState.feedback = "已退出当前测验，回到普通词训范围。";
}

function wrongReviewWords() {
  const progress = readWordTrainerProgress();
  return ENGLISH_WORD_BANK.filter((item) => wordHasWrongRecord(item, progress) && wordMatchesQuery(item)).sort((left, right) => {
    const wrongDiff = wordWrongCount(right, progress) - wordWrongCount(left, progress);
    if (wrongDiff) return wrongDiff;
    return (progress[wordKey(right)]?.lastWrongAt || 0) - (progress[wordKey(left)]?.lastWrongAt || 0);
  });
}

function setWordProgress(item, level, result = "neutral") {
  const progress = readWordTrainerProgress();
  const key = wordKey(item);
  const previous = progress[key] || {};
  progress[key] = {
    ...previous,
    level,
    times: (previous.times || 0) + 1,
    correct: (previous.correct || 0) + (result === "correct" ? 1 : 0),
    wrong: (previous.wrong || 0) + (result === "wrong" ? 1 : 0),
    lastWrongAt: result === "wrong" ? Date.now() : previous.lastWrongAt,
    updatedAt: Date.now(),
  };
  saveWordTrainerProgress(progress);
}

function promoteWordProgress(item, level) {
  setWordProgress(item, Math.max(wordProgressLevel(item), level), "correct");
}

function recordWordMistake(item) {
  setWordProgress(item, Math.min(wordProgressLevel(item), 1), "wrong");
}

function moveWordTrainer(step) {
  const words = wordTrainerWords();
  if (!words.length) return;
  wordTrainerState.index = (wordTrainerState.index + step + words.length) % words.length;
  wordTrainerState.feedback = "";
  wordTrainerState.exampleFeedback = "";
  wordTrainerState.examplePassed = false;
  wordTrainerState.step = 0;
}

function pronunciationText(text) {
  return String(text || "")
    .split("=")[0]
    .replace(/\([^)]*\)/g, "")
    .replace(/[;,/，；].*$/g, "")
    .trim();
}

function escapeRegExp(value) {
  return String(value).replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}

function wordClozeForms(item) {
  const base = pronunciationText(item.word).toLowerCase();
  if (!base || !/^[a-z-]+$/.test(base)) return [];
  const forms = new Set([base]);
  if (base.endsWith("e")) forms.add(`${base.slice(0, -1)}ing`);
  else forms.add(`${base}ing`);
  if (base.endsWith("y")) forms.add(`${base.slice(0, -1)}ies`);
  forms.add(`${base}s`);
  forms.add(`${base}es`);
  forms.add(`${base}ed`);
  if (base.endsWith("e")) forms.add(`${base}d`);
  return [...forms].sort((left, right) => right.length - left.length);
}

function wordClozeData(item) {
  const forms = wordClozeForms(item);
  const example = item.example || "";
  for (const form of forms) {
    const pattern = new RegExp(`\\b${escapeRegExp(form)}\\b`, "i");
    if (pattern.test(example)) {
      return {
        answer: form.toLowerCase(),
        base: pronunciationText(item.word).toLowerCase(),
        text: example.replace(pattern, "______"),
      };
    }
  }
  return {
    answer: pronunciationText(item.word).toLowerCase(),
    base: pronunciationText(item.word).toLowerCase(),
    text: `${example}  →  ______`,
  };
}

function advanceWordTrainerStep(step, feedback) {
  wordTrainerState.step = Math.max(wordTrainerState.step, step);
  if (feedback) wordTrainerState.feedback = feedback;
}

function speakEnglishText(text, { rate = 0.78 } = {}) {
  if (!("speechSynthesis" in window) || typeof SpeechSynthesisUtterance === "undefined") {
    wordTrainerState.feedback = "当前浏览器暂不支持系统朗读，可以换 Chrome / Safari 再试。";
    return false;
  }
  const spokenText = String(text || "").trim();
  if (!spokenText) return false;
  const utterance = new SpeechSynthesisUtterance(spokenText);
  utterance.lang = "en-US";
  utterance.rate = rate;
  utterance.pitch = 1;
  utterance.volume = 1;
  const voices = window.speechSynthesis.getVoices();
  utterance.voice =
    voices.find((voice) => voice.lang === "en-US" && /samantha|daniel|alex|google|microsoft/i.test(voice.name)) ||
    voices.find((voice) => voice.lang?.startsWith("en-US")) ||
    voices.find((voice) => voice.lang?.startsWith("en"));
  window.speechSynthesis.cancel();
  window.speechSynthesis.speak(utterance);
  return true;
}

function speakEnglishWord(item) {
  return speakEnglishText(pronunciationText(item.word), { rate: 0.78 });
}

function speakEnglishExample(item) {
  return speakEnglishText(item.example, { rate: 0.82 });
}

function checkExampleCloze(item) {
  const input = document.querySelector("#wordExampleInput");
  const answer = input?.value.trim().toLowerCase() || "";
  const cloze = wordClozeData(item);
  if (!answer) {
    wordTrainerState.feedback = "";
    wordTrainerState.examplePassed = false;
    wordTrainerState.exampleFeedback = "先把例句空格补上；这一步是在训练“看到语境能调出单词”。";
    return;
  }
  if (answer === cloze.answer || answer === cloze.base) {
    promoteWordProgress(item, 1);
    wordTrainerState.feedback = "";
    wordTrainerState.examplePassed = true;
    wordTrainerState.exampleFeedback = `✅ 例句填空正确：${item.example}`;
  } else {
    recordWordMistake(item);
    wordTrainerState.feedback = "";
    wordTrainerState.examplePassed = false;
    wordTrainerState.exampleFeedback = `❌ 例句里应填 ${cloze.answer}。先读完整句，再回到词汇“${item.phrase}”。`;
  }
}

function checkWordSpelling(item) {
  const input = document.querySelector("#wordSpellingInput");
  const answer = input?.value.trim().toLowerCase() || "";
  const target = item.word.toLowerCase();
  if (!answer) {
    wordTrainerState.feedback = "先默写英文，再点检查；哪怕写错，也是在暴露可修补的漏洞。";
    return;
  }
  if (answer === target) {
    promoteWordProgress(item, 2);
    wordTrainerState.feedback = `✅ 拼写正确：${item.word}。现在把词汇搭配“${item.phrase}”读一遍，锁住语境。`;
  } else {
    recordWordMistake(item);
    wordTrainerState.feedback = `❌ 拼写还差一点：你写的是 ${answer}，正确是 ${item.word}。建议按音节拆开再默写一次。`;
  }
}

function wordTrainerCategoryButtonsHTML() {
  return wordCategories()
    .map((category) => {
      const active = category === wordTrainerState.category ? " is-active" : "";
      return `
        <button class="word-category${active}" type="button" data-word-category="${escapeHTML(category)}">
          ${highlight(category)} <span>${wordCategoryCount(category)}</span>
        </button>
      `;
    })
    .join("");
}

function wordTrainerStatsHTML(words, progress) {
  const allStats = wordProgressStats(ENGLISH_WORD_BANK, progress);
  const visibleStats = wordProgressStats(words, progress);
  return `
    <section class="word-stats">
      <article><strong>${ENGLISH_WORD_BANK.length}</strong><span>总词数</span></article>
      <article><strong>${allStats.known}</strong><span>全库已掌握</span></article>
      <article><strong>${allStats.wrong}</strong><span>错题记录</span></article>
      <article><strong>${visibleStats.fuzzy}</strong><span>当前模糊</span></article>
      <article><strong>${visibleStats.fresh}</strong><span>当前未掌握</span></article>
    </section>
  `;
}

function wordTrainerQuizControlsHTML() {
  const progress = readWordTrainerProgress();
  const wrongCount = ENGLISH_WORD_BANK.filter((item) => wordHasWrongRecord(item, progress)).length;
  const sessionActive = wordTrainerState.sessionKeys.length > 0;
  return `
    <section class="word-quiz-controls">
      <div class="word-quiz-controls__main">
        <label>
          <span>随机测验数量</span>
          <input id="wordQuizCount" type="number" min="1" max="200" value="${wordTrainerState.quizCount || 20}" inputmode="numeric" />
        </label>
        <button type="button" data-word-action="start-random-quiz">随机抽词测验</button>
        <button type="button" data-word-action="review-wrong">错题回顾 <span>${wrongCount}</span></button>
        <button type="button" data-word-action="retry-wrong">错词重点重测</button>
        ${sessionActive ? `<button type="button" data-word-action="end-session">退出测验</button>` : ""}
      </div>
      ${
        sessionActive
          ? `<p class="word-session-note">当前测验：${escapeHTML(wordTrainerState.sessionTitle)} · ${wordTrainerState.sessionKeys.length} 词</p>`
          : `<p class="word-session-note">随机测验会从当前分类/搜索范围内抽取；错词重测会优先抽取历史答错词。</p>`
      }
    </section>
  `;
}

function wordTrainerStepHTML(step) {
  return `
    <div class="word-stepper" aria-label="单词训练步骤">
      ${WORD_TRAINER_STEPS.map((label, index) => {
        const className = index < step ? "is-done" : index === step ? "is-active" : "is-locked";
        return `<span class="${className}"><b>${index + 1}</b>${label}</span>`;
      }).join("")}
    </div>
  `;
}

function wordTrainerStageHTML(current, step, options) {
  const cloze = wordClozeData(current);
  if (step === 0) {
    return `
      <section class="word-stage-grid">
        <article class="word-stage is-active">
          <b>1. 意思辨认：先听音，再选义</b>
          <p>上方释义已隐藏。先点英文读音，再从 4 个选项中选出最贴近的中文意思。</p>
          <div class="word-options">
            ${options
              .map(
                (option) => `
                  <button type="button" data-word-option="${escapeHTML(option)}">${highlight(option)}</button>
                `,
              )
              .join("")}
          </div>
        </article>
      </section>
    `;
  }
  if (step === 1) {
    return `
      <section class="word-stage-grid">
        <article class="word-stage is-active">
          <b>2. 词汇复述：读音、释义、词汇搭配一起说出来</b>
          <p class="word-focus-line">${highlight(current.word)} · ${highlight(current.phonetic || "")}</p>
          <p class="word-focus-line">${highlight(current.phrase)}</p>
          <div class="word-stage-actions">
            <button type="button" data-word-action="speak-word">🔊 播放词汇</button>
            <button type="button" data-word-action="phrase-done">我能用这个词汇口头造句</button>
          </div>
        </article>
      </section>
    `;
  }
  if (step === 2) {
    return `
      <section class="word-stage-grid">
        <article class="word-stage is-active">
          <b>3. 例句填空：从语境里调出单词</b>
          <p class="word-cloze">${highlight(cloze.text)}</p>
          <div class="word-spell">
            <input id="wordExampleInput" type="text" placeholder="填空：输入原词或句中形式" autocomplete="off" />
            <button type="button" data-word-action="check-example">检查例句</button>
          </div>
          ${
            wordTrainerState.exampleFeedback
              ? `<div class="word-inline-feedback" role="status">
                  ${escapeHTML(wordTrainerState.exampleFeedback)}
                  ${
                    wordTrainerState.examplePassed
                      ? `<div class="word-stage-actions">
                          <button type="button" data-word-action="speak-example">🔊 播放完整句子</button>
                          <button type="button" data-word-action="example-next">进入助记复述</button>
                        </div>`
                      : ""
                  }
                </div>`
              : ""
          }
        </article>
      </section>
    `;
  }
  if (step === 3) {
    return `
      <section class="word-stage-grid">
        <article class="word-stage is-active">
          <b>4. 助记复述：用一个钩子把它挂住</b>
          <p>${highlight(current.mnemonic)}</p>
          <button type="button" data-word-action="mnemonic-done">我已闭眼复述这个钩子</button>
        </article>
      </section>
    `;
  }
  return `
    <section class="word-stage-grid">
      <article class="word-stage is-active">
        <b>5. 拼写确认：最后一次主动回忆</b>
        <div class="word-spell">
          <input id="wordSpellingInput" type="text" placeholder="默写：${escapeHTML(current.meaning)} 对应的英文" autocomplete="off" />
          <button type="button" data-word-action="check-spelling">检查拼写并计入熟练度</button>
        </div>
      </article>
    </section>
  `;
}

function wordTrainerCardHTML(current, words, progress) {
  const itemProgress = progress[wordKey(current)] || {};
  const level = itemProgress.level || 0;
  const options = wordQuizOptions(current);
  const step = Math.min(Math.max(wordTrainerState.step || 0, 0), WORD_TRAINER_STEPS.length - 1);
  return `
    <article class="word-card">
      <div class="word-card__top">
        <span class="mindmap-root__tag">${highlight(current.category)}</span>
        <span class="word-level ${wordLevelClass(level)}">${wordLevelLabel(level)} · ${itemProgress.times || 0} 次训练${
          itemProgress.wrong ? ` · 错 ${itemProgress.wrong}` : ""
        }</span>
      </div>
      <div class="word-card__main">
        <span class="word-index">${wordTrainerState.index + 1} / ${words.length}</span>
        <h3>
          <button class="word-title-audio" type="button" data-word-action="speak-word" aria-label="播放 ${escapeHTML(current.word)} 的英文读音" title="点击播放单词读音">
            <span>${highlight(current.word)}</span>
            <small>🔊 点单词播放读音</small>
          </button>
        </h3>
        <p class="word-phonetic">${highlight(current.phonetic || "音标待补充")}</p>
        ${
          step === 0
            ? `<p class="word-meaning word-meaning--locked">释义已隐藏：先完成“意思辨认”再显示</p>`
            : `<p class="word-meaning">${highlight(current.meaning)}</p>`
        }
      </div>
      ${wordTrainerStepHTML(step)}
      ${wordTrainerStageHTML(current, step, options)}
      ${
        wordTrainerState.feedback
          ? `<div class="word-feedback" role="status">${escapeHTML(wordTrainerState.feedback)}</div>`
          : ""
      }
      <div class="word-actions">
        <button type="button" data-word-action="prev">上一词</button>
        <button type="button" data-word-action="next">下一词</button>
        <button type="button" data-word-action="mark-new">还不会</button>
        <button type="button" data-word-action="mark-fuzzy">有点模糊</button>
        <button type="button" data-word-action="mark-known">已掌握</button>
        <button type="button" data-word-action="reset-progress">重置词训记录</button>
      </div>
    </article>
  `;
}

function renderWordTrainer() {
  hideVolumeTabs();
  const words = currentWordList();
  const progress = readWordTrainerProgress();
  const allStats = wordProgressStats(ENGLISH_WORD_BANK, progress);
  breadcrumb.innerHTML = `
    <div>
      <h2>英语必掌握单词训练</h2>
      <p>${
        state.query.trim()
          ? `关键词「${escapeHTML(state.query)}」匹配 ${words.length} 个词`
          : "音标跟读 · 选择辨义 · 词汇复述 · 例句填空 · 拼写确认"
      }</p>
    </div>
    <div class="progress-ring">
      <strong>${allStats.known}/${ENGLISH_WORD_BANK.length}</strong>
      <span>已掌握</span>
    </div>
  `;

  unitList.innerHTML = `
    <div class="word-trainer">
      <article class="word-hero">
        <span class="mindmap-root__tag">高考英语 · 核心词汇闭环</span>
        <h3>按记忆规律逐步解锁，不再假装“看过就会”</h3>
        <p>每个词必须完成：听音辨义 → 词汇复述 → 例句填空 → 助记复述 → 闭卷拼写。拼写正确才进入“已掌握”。</p>
      </article>
      <section class="word-category-row">${wordTrainerCategoryButtonsHTML()}</section>
      ${wordTrainerQuizControlsHTML()}
      ${wordTrainerStatsHTML(words, progress)}
      ${
        words.length
          ? wordTrainerCardHTML(words[wordTrainerState.index], words, progress)
          : `
            <div class="empty">
              <strong>当前没有匹配单词</strong>
              <p>可以清空搜索，或换“阅读”“写作”“完形”“话题”等关键词。</p>
            </div>
          `
      }
    </div>
  `;
}

function handleWordTrainerClick(event) {
  const categoryButton = event.target.closest("[data-word-category]");
  if (categoryButton) {
    wordTrainerState.category = categoryButton.dataset.wordCategory;
    wordTrainerState.sessionKeys = [];
    wordTrainerState.sessionTitle = "";
    wordTrainerState.index = 0;
    wordTrainerState.feedback = "";
    wordTrainerState.exampleFeedback = "";
    wordTrainerState.examplePassed = false;
    wordTrainerState.step = 0;
    render();
    return true;
  }

  const optionButton = event.target.closest("[data-word-option]");
  if (optionButton) {
    const words = currentWordList();
    const current = words[wordTrainerState.index];
    if (!current) return true;
    const chosen = optionButton.dataset.wordOption;
    if (chosen === current.meaning) {
      promoteWordProgress(current, 1);
      wordTrainerState.exampleFeedback = "";
      wordTrainerState.examplePassed = false;
      advanceWordTrainerStep(1, `✅ 选对了：${current.word} = ${current.meaning}。现在进入词汇复述。`);
    } else {
      recordWordMistake(current);
      wordTrainerState.feedback = `❌ 这次选成了“${chosen}”，正确是“${current.meaning}”。看一眼助记钩子再来。`;
    }
    render();
    return true;
  }

  const actionButton = event.target.closest("[data-word-action]");
  if (!actionButton) return false;
  const words = currentWordList();
  const current = words[wordTrainerState.index];
  const action = actionButton.dataset.wordAction;
  const globalActions = new Set(["reset-progress", "start-random-quiz", "review-wrong", "retry-wrong", "end-session"]);
  if (!current && !globalActions.has(action)) return true;

  switch (action) {
    case "start-random-quiz": {
      const count = readWordQuizCount();
      const sample = shuffledSample(baseWordTrainerWords(), count);
      startWordQuizSession(sample, `随机抽词测验 ${sample.length}/${count}`);
      break;
    }
    case "review-wrong": {
      wordTrainerState.sessionKeys = [];
      wordTrainerState.sessionTitle = "";
      wordTrainerState.category = "错题回顾";
      wordTrainerState.index = 0;
      resetWordTrainerTransientState();
      const wrongCount = wordCategoryCount("错题回顾");
      wordTrainerState.feedback = wrongCount ? `已进入错题回顾，共 ${wrongCount} 个历史错词。` : "目前还没有错词记录。";
      break;
    }
    case "retry-wrong": {
      const count = readWordQuizCount();
      const sample = shuffledSample(wrongReviewWords(), count);
      startWordQuizSession(sample, `错词重点重测 ${sample.length}/${count}`);
      break;
    }
    case "end-session":
      endWordQuizSession();
      break;
    case "prev":
      moveWordTrainer(-1);
      break;
    case "next":
      moveWordTrainer(1);
      break;
    case "speak-word":
      if (!speakEnglishWord(current)) render();
      return true;
    case "phrase-done":
      wordTrainerState.exampleFeedback = "";
      wordTrainerState.examplePassed = false;
      advanceWordTrainerStep(2, `好，词汇复述已过。现在把例句里的空格补出来：${current.phrase}`);
      break;
    case "check-example":
      checkExampleCloze(current);
      break;
    case "speak-example":
      if (!speakEnglishExample(current)) render();
      return true;
    case "example-next":
      wordTrainerState.exampleFeedback = "";
      wordTrainerState.examplePassed = false;
      advanceWordTrainerStep(3, "");
      break;
    case "mnemonic-done":
      advanceWordTrainerStep(4, "助记钩子已复述。最后闭卷拼写一次，正确后计入“已掌握”。");
      break;
    case "mark-new":
      setWordProgress(current, 0);
      wordTrainerState.feedback = `已把 ${current.word} 标为“还不会”，它会留在复习重点里。`;
      break;
    case "mark-fuzzy":
      setWordProgress(current, 1);
      wordTrainerState.feedback = `已把 ${current.word} 标为“有点模糊”，建议明天再刷一次。`;
      break;
    case "mark-known":
      setWordProgress(current, 2);
      wordTrainerState.feedback = `漂亮，${current.word} 已标为掌握。别飘，隔几天还要抽查。`;
      break;
    case "check-spelling":
      checkWordSpelling(current);
      break;
    case "reset-progress":
      localStorage.removeItem(WORD_TRAINER_STORAGE_KEY);
      wordTrainerState.sessionKeys = [];
      wordTrainerState.sessionTitle = "";
      resetWordTrainerTransientState();
      wordTrainerState.feedback = "词训记录已重置，可以重新开始一轮干净的记忆闭环。";
      break;
    default:
      return false;
  }
  render();
  return true;
}

function biliSearchUrl(keyword) {
  return `https://search.bilibili.com/all?keyword=${encodeURIComponent(keyword)}`;
}

function biliUpSearchUrl(keyword) {
  return `https://search.bilibili.com/upuser?keyword=${encodeURIComponent(keyword)}`;
}

function progressForSubject(subjectIndex) {
  const subject = COURSE[subjectIndex];
  const total = subject.volumes.reduce((sum, volume) => sum + volume.units.length, 0);
  let done = 0;
  subject.volumes.forEach((volume, volumeIndex) => {
    volume.units.forEach((_, unitIndex) => {
      if (doneSet.has(keyFor(subjectIndex, volumeIndex, unitIndex))) done += 1;
    });
  });
  return { total, done, percent: total ? Math.round((done / total) * 100) : 0 };
}

function renderStats() {
  const totals = courseTotals();
  const percent = totals.units ? Math.round((totals.done / totals.units) * 100) : 0;
  stats.innerHTML = [
    ["学科", totals.subjects],
    ["册别", totals.volumes],
    ["单元 / 章节 / 课", totals.units],
    ["知识导图节点", totals.knowledgeNodes],
    ["学习方法", `${LEARNING_METHODS.length} 板块`],
    ["英语词训", `${ENGLISH_WORD_BANK.length} 词`],
    ["B站资源", `${BILIBILI_VIDEO_RESOURCES.length} 科`],
    ["完成进度", `${percent}%`],
  ]
    .map(
      ([label, value]) => `
        <article class="stat-card">
          <strong>${value}</strong>
          <span>${label}</span>
        </article>
      `,
    )
    .join("");
}

function renderModeSwitch() {
  modeSwitch.querySelectorAll("[data-mode]").forEach((button) => {
    button.classList.toggle("is-active", button.dataset.mode === state.mode);
  });
}

function renderSubjects() {
  subjectCount.textContent = `${COURSE.length} 科`;
  subjectList.innerHTML = COURSE.map((subject, subjectIndex) => {
    const progress = progressForSubject(subjectIndex);
    const active =
      subjectIndex === state.subjectIndex &&
      !["methods", "wordTrainer"].includes(state.mode) &&
      (state.mode === "videos" || !state.query)
        ? " is-active"
        : "";
    return `
      <button class="subject-btn${active}" type="button" data-subject-index="${subjectIndex}">
        <b>${escapeHTML(subject.name)}</b>
        <span>${progress.done}/${progress.total}</span>
      </button>
    `;
  }).join("");
}

function renderBreadcrumb(subjectIndex = state.subjectIndex) {
  const subject = COURSE[subjectIndex];
  const progress = progressForSubject(subjectIndex);
  breadcrumb.innerHTML = `
    <div>
      <h2>${escapeHTML(subject.name)}</h2>
      <p>${subject.volumes.length} 个册别 · ${progress.total} 个学习单元</p>
    </div>
    <div class="progress-ring">
      <strong>${progress.percent}%</strong>
      <span>${progress.done}/${progress.total} 已学</span>
    </div>
  `;
}

function renderVolumeTabs() {
  const subject = COURSE[state.subjectIndex];
  volumeTabs.hidden = false;
  volumeTabs.innerHTML = subject.volumes
    .map((volume, volumeIndex) => {
      const active = volumeIndex === state.volumeIndex ? " is-active" : "";
      return `
        <button class="volume-tab${active}" type="button" data-volume-index="${volumeIndex}">
          ${escapeHTML(volume.name)}
        </button>
      `;
    })
    .join("");
}

function hideVolumeTabs() {
  volumeTabs.hidden = true;
  volumeTabs.innerHTML = "";
}

function unitHTML(subjectIndex, volumeIndex, unit, unitIndex, volumeName) {
  const id = keyFor(subjectIndex, volumeIndex, unitIndex);
  const checked = doneSet.has(id);
  return `
    <label class="unit${checked ? " is-done" : ""}">
      <input type="checkbox" data-progress-key="${id}" ${checked ? "checked" : ""} />
      <span class="unit__title">${highlight(unit)}</span>
      <span class="unit__meta">${escapeHTML(volumeName)}</span>
    </label>
  `;
}

function volumeCardHTML(subjectIndex, volumeIndex, matchedUnits = null) {
  const volume = COURSE[subjectIndex].volumes[volumeIndex];
  const units = matchedUnits || volume.units.map((unit, unitIndex) => ({ unit, unitIndex }));
  return `
    <article class="volume-card">
      <div class="volume-card__head">
        <h3>${highlight(volume.name)}</h3>
        <small>${units.length} 项</small>
      </div>
      <div class="units">
        ${units
          .map(({ unit, unitIndex }) => unitHTML(subjectIndex, volumeIndex, unit, unitIndex, volume.name))
          .join("")}
      </div>
    </article>
  `;
}

function mindMapBranchHTML(branch, branchIndex) {
  const open = branchIndex < 3 ? " open" : "";
  return `
    <details class="mindmap-branch"${open}>
      <summary>${highlight(branch.title)}</summary>
      <ul class="mindmap-points">
        ${branch.points.map((point) => `<li>${highlight(point)}</li>`).join("")}
      </ul>
    </details>
  `;
}

function mindMapCardHTML(subjectIndex, volumeIndex, unitIndex) {
  const map = buildMindMap(subjectIndex, volumeIndex, unitIndex);
  return `
    <article class="mindmap-card">
      <div class="mindmap-root">
        <span class="mindmap-root__tag">知识思维导图 · ${countMindMapNodes(map)} 节点</span>
        <h3>${highlight(map.title)}</h3>
        <p>${highlight(map.subtitle)} · 学习目标、核心知识、方法模型、题型应用、易错诊断与专题深化</p>
      </div>
      <div class="mindmap-branches">
        ${map.branches.map((branch, branchIndex) => mindMapBranchHTML(branch, branchIndex)).join("")}
      </div>
    </article>
  `;
}

function mindMapListHTML(subjectIndex, volumeIndex, matchedUnits = null) {
  const volume = COURSE[subjectIndex].volumes[volumeIndex];
  const units = matchedUnits || volume.units.map((unit, unitIndex) => ({ unit, unitIndex }));
  return `
    <div class="mindmap-list">
      ${units.map(({ unitIndex }) => mindMapCardHTML(subjectIndex, volumeIndex, unitIndex)).join("")}
    </div>
  `;
}

function networkUnitChipHTML(item) {
  return `
    <button
      class="network-unit"
      type="button"
      data-go-unit="${item.subjectIndex}-${item.volumeIndex}-${item.unitIndex}"
      title="${escapeHTML(item.volumeName)}"
    >
      <span>${highlight(item.unit)}</span>
      <small>${escapeHTML(item.volumeName)}</small>
    </button>
  `;
}

function compactPointListHTML(points) {
  return `
    <ul class="network-points">
      ${points.map((point) => `<li>${highlight(point)}</li>`).join("")}
    </ul>
  `;
}

function subjectVolumeMapHTML(subjectIndex) {
  const subject = COURSE[subjectIndex];
  return `
    <section class="subject-network-section">
      <div class="subject-network-section__head">
        <h3>分册—章节全量节点图</h3>
        <p>覆盖本学科所有册别与全部章节，作为跨册关系的底图。</p>
      </div>
      <div class="volume-roadmap">
        ${subject.volumes
          .map(
            (volume, volumeIndex) => `
              <details class="volume-roadmap__item"${volumeIndex < 2 ? " open" : ""}>
                <summary>
                  <strong>${highlight(volume.name)}</strong>
                  <span>${volume.units.length} 个节点</span>
                </summary>
                <div class="network-units">
                  ${volume.units
                    .map((unit, unitIndex) =>
                      networkUnitChipHTML({
                        subjectIndex,
                        volumeIndex,
                        unitIndex,
                        volumeName: volume.name,
                        unit,
                      }),
                    )
                    .join("")}
                </div>
              </details>
            `,
          )
          .join("")}
      </div>
    </section>
  `;
}

function subjectRelationHTML(subjectIndex) {
  const relations = subjectRelations(subjectIndex);
  return `
    <section class="subject-network-section">
      <div class="subject-network-section__head">
        <h3>跨章节 / 跨分册关联链</h3>
        <p>把先修知识、方法迁移和综合考点连成可复习的路径。</p>
      </div>
      <div class="relation-grid">
        ${relations
          .map(
            (relation) => `
              <article class="relation-card">
                <div class="relation-card__head">
                  <h4>${highlight(relation.title)}</h4>
                  <p>${highlight(relation.note)}</p>
                </div>
                <div class="relation-chain" aria-label="${escapeHTML(relation.title)}关键词链">
                  ${relation.nodes.map((node) => `<span>${highlight(node)}</span>`).join("")}
                </div>
                <details class="relation-units" open>
                  <summary>关联章节 ${relation.units.length} 个</summary>
                  <div class="network-units">
                    ${
                      relation.units.length
                        ? relation.units.map((item) => networkUnitChipHTML(item)).join("")
                        : `<p class="mindmap-empty-note">当前目录中没有直接命中节点，可作为方法迁移线索使用。</p>`
                    }
                  </div>
                </details>
              </article>
            `,
          )
          .join("")}
      </div>
    </section>
  `;
}

function subjectTopicGroupsHTML(subjectIndex) {
  const topics = topicGroupsForSubject(subjectIndex);
  return `
    <section class="subject-network-section">
      <div class="subject-network-section__head">
        <h3>跨册专题群</h3>
        <p>按专题关键词聚合不同册别中的同类知识点，适合二轮复习和综合训练。</p>
      </div>
      <div class="topic-grid">
        ${topics
          .map(
            (topic) => `
              <details class="topic-card">
                <summary>
                  <strong>${highlight(topic.title)}</strong>
                  <span>${topic.units.length} 个章节</span>
                </summary>
                ${compactPointListHTML(topic.points)}
                <div class="network-units">
                  ${topic.units.map((item) => networkUnitChipHTML(item)).join("")}
                </div>
              </details>
            `,
          )
          .join("")}
      </div>
    </section>
  `;
}

function subjectMethodLatticeHTML(subjectIndex) {
  const subject = COURSE[subjectIndex];
  const guideKey = normalizeSubjectKey(subject.name);
  const guide = SUBJECT_GUIDES[guideKey] || SUBJECT_GUIDES.语文;
  const blocks = [
    ["学科主线", guide.core],
    ["方法迁移", guide.methods],
    ["综合题型", guide.exam],
    ["易错诊断", guide.errors],
  ];
  return `
    <section class="subject-network-section">
      <div class="subject-network-section__head">
        <h3>方法与能力贯通层</h3>
        <p>这些不是某一章独有的点，而是贯穿整门学科的通用能力。</p>
      </div>
      <div class="lattice-grid">
        ${blocks
          .map(
            ([title, points]) => `
              <article class="lattice-card">
                <h4>${highlight(title)}</h4>
                ${compactPointListHTML(points)}
              </article>
            `,
          )
          .join("")}
      </div>
    </section>
  `;
}

function subjectNetworkHTML(subjectIndex) {
  const subject = COURSE[subjectIndex];
  const unitCount = allUnitsForSubject(subjectIndex).length;
  const relationCount = subjectRelations(subjectIndex).length;
  const topicCount = topicGroupsForSubject(subjectIndex).length;
  return `
    <div class="subject-network">
      <article class="subject-network-hero">
        <span class="mindmap-root__tag">跨章节 · 跨分册 · 学科总图</span>
        <h3>${highlight(subject.name)}全册知识关系网</h3>
        <p>
          覆盖 ${subject.volumes.length} 个册别、${unitCount} 个章节节点、${relationCount} 条跨册主链、
          ${topicCount} 个专题群，共 ${countSubjectNetworkNodes(subjectIndex)} 个总图节点。
        </p>
      </article>
      ${subjectVolumeMapHTML(subjectIndex)}
      ${subjectRelationHTML(subjectIndex)}
      ${subjectTopicGroupsHTML(subjectIndex)}
      ${subjectMethodLatticeHTML(subjectIndex)}
    </div>
  `;
}

function methodSearchFields(block) {
  return [
    block.title,
    block.tag,
    block.summary,
    ...block.steps.flatMap((step) => [step.name, step.time, step.action, ...step.tips]),
    ...block.checklist,
  ];
}

function methodMatches(block) {
  return containsQuery(...methodSearchFields(block));
}

function methodStepHTML(step, index) {
  return `
    <article class="method-step">
      <span class="method-step__num">${index + 1}</span>
      <div>
        <div class="method-step__head">
          <h4>${highlight(step.name)}</h4>
          <small>${highlight(step.time)}</small>
        </div>
        <p>${highlight(step.action)}</p>
        <ul>
          ${step.tips.map((tip) => `<li>${highlight(tip)}</li>`).join("")}
        </ul>
      </div>
    </article>
  `;
}

function learningMethodCardHTML(block, index) {
  return `
    <article class="method-card">
      <div class="method-card__head">
        <span class="mindmap-root__tag">方法 ${index + 1}</span>
        <h3>${highlight(block.title)}</h3>
        <p class="method-card__tagline">${highlight(block.tag)}</p>
        <p>${highlight(block.summary)}</p>
      </div>
      <div class="method-steps">
        ${block.steps.map((step, stepIndex) => methodStepHTML(step, stepIndex)).join("")}
      </div>
      <details class="method-checklist" open>
        <summary>今天照着问自己的 3 个问题</summary>
        <ul>
          ${block.checklist.map((item) => `<li>${highlight(item)}</li>`).join("")}
        </ul>
      </details>
    </article>
  `;
}

function dailyFlowHTML() {
  return `
    <section class="method-section">
      <div class="method-section__head">
        <span class="mindmap-root__tag">可直接照搬</span>
        <h3>普通学生每日执行流程</h3>
        <p>不用一次变完美，先按这 5 步执行一周：预习有问题、课堂抓逻辑、课后讲出来、错题真复盘、睡前做收口。</p>
      </div>
      <div class="daily-flow">
        ${DAILY_METHOD_FLOW.map(
          ([time, action]) => `
            <article>
              <strong>${highlight(time)}</strong>
              <span>${highlight(action)}</span>
            </article>
          `,
        ).join("")}
      </div>
    </section>
  `;
}

function wrongQuestionTemplateHTML() {
  return `
    <section class="method-section">
      <div class="method-section__head">
        <span class="mindmap-root__tag">提分快捷入口</span>
        <h3>错题复盘极简模板</h3>
        <p>错题本不要抄题抄答案，照下面 5 行写，目标是杜绝同错反复犯。</p>
      </div>
      <ol class="wrong-template">
        ${WRONG_QUESTION_TEMPLATE.map((item) => `<li>${highlight(item)}</li>`).join("")}
      </ol>
    </section>
  `;
}

function weeklyRhythmHTML() {
  return `
    <section class="method-section">
      <div class="method-section__head">
        <span class="mindmap-root__tag">稳定提分</span>
        <h3>一周执行节奏</h3>
        <p>把学习方法放进固定节奏里，才不会变成看完很燃、第二天忘光。</p>
      </div>
      <div class="weekly-grid">
        <article>
          <b>周一到周五</b>
          <p>课前轻预习，课上当堂清，晚上用番茄钟完成复盘和核心题。</p>
        </article>
        <article>
          <b>周三小检查</b>
          <p>检查本周错题是否都写了错因；薄弱点先补 1 个，不贪多。</p>
        </article>
        <article>
          <b>周末总复盘</b>
          <p>整理一张本周知识框架图，集中翻看错题，再找同类题验证。</p>
        </article>
        <article>
          <b>每次考试后</b>
          <p>不急着看排名，先做漏洞清单：知识漏洞、方法漏洞、状态漏洞。</p>
        </article>
      </div>
    </section>
  `;
}

function learningMethodsHTML(blocks) {
  return `
    <div class="learning-methods">
      <article class="method-hero">
        <span class="mindmap-root__tag">学霸高效学习精髓</span>
        <h3>不是拼天赋，是拼标准流程</h3>
        <p>
          把知识学习、刷题复盘、时间管理、身心调节做成可照搬的行动清单。
          每个学生都能从“假努力”切换到“有反馈的有效学习”。
        </p>
      </article>
      ${dailyFlowHTML()}
      <section class="method-grid">
        ${blocks.map((block, index) => learningMethodCardHTML(block, index)).join("")}
      </section>
      ${wrongQuestionTemplateHTML()}
      ${weeklyRhythmHTML()}
    </div>
  `;
}

function renderLearningMethods() {
  const blocks = LEARNING_METHODS.filter(methodMatches);
  hideVolumeTabs();
  breadcrumb.innerHTML = `
    <div>
      <h2>学习方法</h2>
      <p>${state.query.trim() ? `关键词「${escapeHTML(state.query)}」匹配 ${blocks.length} 个方法板块` : "知识学习 · 刷题复盘 · 时间管理 · 身心调节"}</p>
    </div>
    <div class="progress-ring">
      <strong>${LEARNING_METHODS.length}</strong>
      <span>方法板块</span>
    </div>
  `;

  if (!blocks.length) {
    unitList.innerHTML = `
      <div class="empty">
        <div>
          <strong>没有找到匹配的方法</strong>
          <p>可以搜索“预习”“错题”“番茄钟”“焦虑”“复盘”等关键词。</p>
        </div>
      </div>
    `;
    return;
  }

  unitList.innerHTML = learningMethodsHTML(blocks);
}

function videoResourceSearchFields(resource) {
  return [
    resource.subject,
    resource.curator,
    resource.role,
    resource.why,
    resource.note,
    ...(resource.watchOrder || []),
    ...(resource.keywords || []),
    ...videoResourceSyllabusFields(resource),
    ...upRecommendationFields(resource),
    resource.secondary?.name || "",
    resource.secondary?.reason || "",
  ];
}

function videoResourceMatches(resource) {
  return containsQuery(...videoResourceSearchFields(resource));
}

function videoResourceForSubject(subjectIndex) {
  return BILIBILI_VIDEO_RESOURCES.find((resource) => subjectIndexForVideoResource(resource) === subjectIndex);
}

function subjectIndexForVideoResource(resource) {
  const exactIndex = COURSE.findIndex((subject) => subject.name === resource.subject);
  if (exactIndex >= 0) return exactIndex;
  return COURSE.findIndex(
    (subject) => subject.name.includes(resource.subject) || resource.subject.includes(subject.name),
  );
}

function videoResourceSyllabusFields(resource) {
  const subjectIndex = subjectIndexForVideoResource(resource);
  if (subjectIndex < 0) return [];
  const subject = COURSE[subjectIndex];
  return [
    subject.name,
    ...subject.volumes.flatMap((volume) => [volume.name, ...volume.units]),
  ];
}

function upRecommendationFields(resource) {
  return (resource.recommendations || []).flatMap((item) => [
    item.name,
    item.level,
    item.focus,
    item.fit,
    item.summary,
  ]);
}

function orderedVideoResources(resources) {
  return [...resources].sort((left, right) => {
    const leftIndex = subjectIndexForVideoResource(left);
    const rightIndex = subjectIndexForVideoResource(right);
    return (leftIndex < 0 ? 999 : leftIndex) - (rightIndex < 0 ? 999 : rightIndex);
  });
}

function videoSearchKeyword(resource, volumeName, unitName) {
  return (unitName || volumeName || resource.subject).trim();
}

function videoVolumeSearchUrl(resource, volumeIndex = state.volumeIndex) {
  const subjectIndex = subjectIndexForVideoResource(resource);
  const subject = subjectIndex >= 0 ? COURSE[subjectIndex] : null;
  const volume = subject?.volumes[volumeIndex] || subject?.volumes[0];
  return biliSearchUrl(videoSearchKeyword(resource, volume?.name || "", ""));
}

function videoVolumeMatchesQuery(resource, subject, volume, unit) {
  const q = state.query.trim();
  if (!q) return true;
  return containsQuery(
    resource.subject,
    resource.curator,
    resource.role,
    resource.why,
    resource.note,
    subject.name,
    volume.name,
    unit,
    ...(resource.watchOrder || []),
    ...(resource.keywords || []),
    ...upRecommendationFields(resource),
  );
}

function videoSyllabusLinksHTML(resource, volumeIndex = state.volumeIndex) {
  const subjectIndex = subjectIndexForVideoResource(resource);
  if (subjectIndex < 0) return "";
  const subject = COURSE[subjectIndex];
  const safeVolumeIndex = Math.min(Math.max(volumeIndex, 0), subject.volumes.length - 1);
  const volume = subject.volumes[safeVolumeIndex];
  const units = volume.units.filter((unit) => videoVolumeMatchesQuery(resource, subject, volume, unit));
  return `
    <section class="video-syllabus">
      <h4>按教材顺序找视频</h4>
      <p>当前内容跟随上方分册按钮：${highlight(subject.name)} · ${highlight(volume.name)}。点击章节标题时只用该章节 / 单元标题在 B站检索，避免关键词过长导致找不到视频。</p>
      <div class="video-syllabus__volumes">
        <details open>
          <summary>
            <b>${highlight(volume.name)}</b>
            <span>${units.length}/${volume.units.length} 项</span>
          </summary>
          <div class="video-unit-links">
            ${
              units.length
                ? units
                    .map((unit) => {
                      const keyword = videoSearchKeyword(resource, volume.name, unit);
                      return `
                        <a href="${escapeHTML(biliSearchUrl(keyword))}" target="_blank" rel="noopener noreferrer">
                          ${highlight(unit)}
                        </a>
                      `;
                    })
                    .join("")
                : `
                  <div class="video-empty">
                    当前分册没有匹配「${escapeHTML(state.query)}」的章节，可清空搜索或切换分册。
                  </div>
                `
            }
          </div>
        </details>
      </div>
    </section>
  `;
}

function resourceTagsHTML(keywords) {
  return `
    <div class="video-tags">
      ${(keywords || []).map((keyword) => `<span>${highlight(keyword)}</span>`).join("")}
    </div>
  `;
}

function resourceWatchOrderHTML(steps) {
  return `
    <ol class="video-watch-order">
      ${(steps || []).map((step) => `<li>${highlight(step)}</li>`).join("")}
    </ol>
  `;
}

function upRecommendationsHTML(resource) {
  const items = resource.recommendations || [];
  if (!items.length) return "";
  return `
    <section class="video-up-section">
      <h4>分档 UP 推荐</h4>
      <div class="video-up-grid">
        ${items
          .map(
            (item) => `
              <article class="video-up-card">
                <div class="video-up-card__head">
                  <strong>${highlight(item.name)}</strong>
                  <span>${highlight(item.level)}</span>
                </div>
                <p><b>主攻：</b>${highlight(item.focus)}</p>
                <p><b>适合：</b>${highlight(item.fit)}</p>
                <p>${highlight(item.summary)}</p>
                <a href="${escapeHTML(biliUpSearchUrl(item.name))}" target="_blank" rel="noopener noreferrer">搜索 UP</a>
              </article>
            `,
          )
          .join("")}
      </div>
    </section>
  `;
}

function videoResourceCardHTML(resource, volumeIndex = state.volumeIndex) {
  return `
    <article class="video-card">
      <div class="video-card__head">
        <span class="mindmap-root__tag">${highlight(resource.subject)}</span>
        <h3>${highlight(resource.curator)}</h3>
        <p class="video-card__role">${highlight(resource.role)}</p>
      </div>
      <div class="video-card__body">
        <p>${highlight(resource.why)}</p>
        ${resourceTagsHTML(resource.keywords)}
        ${upRecommendationsHTML(resource)}
        <section class="video-watch">
          <h4>建议观看顺序</h4>
          ${resourceWatchOrderHTML(resource.watchOrder)}
        </section>
        ${videoSyllabusLinksHTML(resource, volumeIndex)}
        ${
          resource.secondary
            ? `
              <aside class="video-secondary">
                <strong>补充资源：${highlight(resource.secondary.name)}</strong>
                <p>${highlight(resource.secondary.reason)}</p>
                <a href="${escapeHTML(resource.secondary.url)}" target="_blank" rel="noopener noreferrer">打开补充 UP 主页</a>
              </aside>
            `
            : ""
        }
        <p class="video-card__note">${highlight(resource.note)}</p>
      </div>
      <div class="video-card__actions">
        <a href="${escapeHTML(resource.homepage)}" target="_blank" rel="noopener noreferrer">UP主页</a>
        <a href="${escapeHTML(videoVolumeSearchUrl(resource, volumeIndex))}" target="_blank" rel="noopener noreferrer">当前分册检索</a>
      </div>
    </article>
  `;
}

function bilibiliVideosHTML(resource, volumeIndex = state.volumeIndex) {
  const subjectIndex = subjectIndexForVideoResource(resource);
  const subject = subjectIndex >= 0 ? COURSE[subjectIndex] : null;
  const volume = subject?.volumes[volumeIndex] || subject?.volumes[0];
  return `
    <div class="bilibili-resources">
      <article class="video-hero">
        <span class="mindmap-root__tag">Bilibili 学科视频集</span>
        <h3>${subject ? `${escapeHTML(subject.name)} · ${escapeHTML(volume.name)}` : "优质 UP / 视频集入口"}</h3>
        <p>
          上方分册按钮与本学科真实目录保持一致，下方章节检索入口只对应当前分册。
          点击后会跳转到 B站原页面，适合按教材进度做课外补充、概念重建和素材积累。
        </p>
      </article>
      <section class="video-guide">
        <article>
          <strong>怎么用</strong>
          <p>先看当前章节对应专题，再回到 H5 的知识导图和错题复盘，不要把视频当成被动刷剧。</p>
        </article>
        <article>
          <strong>怎么筛</strong>
          <p>优先选择播放量高、弹幕/评论反馈好、合集结构完整、更新较稳定的视频集。</p>
        </article>
        <article>
          <strong>怎么复盘</strong>
          <p>每看完 1 个视频，用 3 句话写出“学到什么、对应课本哪里、能解哪类题”。</p>
        </article>
      </section>
      <section class="video-grid">
        ${videoResourceCardHTML(resource, volumeIndex)}
      </section>
    </div>
  `;
}

function renderBilibiliVideos() {
  const subject = COURSE[state.subjectIndex];
  const resource = videoResourceForSubject(state.subjectIndex);
  renderVolumeTabs();
  breadcrumb.innerHTML = `
    <div>
      <h2>${escapeHTML(subject.name)} · B站视频资源</h2>
      <p>${subject.volumes[state.volumeIndex]?.name || subject.volumes[0]?.name || "当前分册"} · ${state.query.trim() ? `按关键词「${escapeHTML(state.query)}」筛选章节` : "分册按钮与实际课程目录一致"}</p>
    </div>
    <div class="progress-ring">
      <strong>${subject.volumes.length}</strong>
      <span>册别</span>
    </div>
  `;

  if (!resource) {
    unitList.innerHTML = `
      <div class="empty">
        <div>
          <strong>当前学科还没有配置 B站视频资源</strong>
          <p>可以先切换到其他学科，或后续为该学科补充 UP / 视频集入口。</p>
        </div>
      </div>
    `;
    return;
  }

  unitList.innerHTML = bilibiliVideosHTML(resource, state.volumeIndex);
}

function renderCurrentVolume() {
  if (state.mode === "methods") {
    renderLearningMethods();
    return;
  }

  if (state.mode === "wordTrainer") {
    renderWordTrainer();
    return;
  }

  if (state.mode === "videos") {
    renderBilibiliVideos();
    return;
  }

  renderBreadcrumb();
  volumeTabs.hidden = state.mode === "subjectMap";
  if (state.mode !== "subjectMap") renderVolumeTabs();
  unitList.innerHTML =
    state.mode === "subjectMap"
      ? subjectNetworkHTML(state.subjectIndex)
      : state.mode === "mindmap"
      ? mindMapListHTML(state.subjectIndex, state.volumeIndex)
      : volumeCardHTML(state.subjectIndex, state.volumeIndex);
}

function getSearchResults() {
  const groups = [];
  COURSE.forEach((subject, subjectIndex) => {
    subject.volumes.forEach((volume, volumeIndex) => {
      const matchedUnits = volume.units
        .map((unit, unitIndex) => ({ unit, unitIndex }))
        .filter(({ unit }) => containsQuery(subject.name, volume.name, unit));

      if (matchedUnits.length) {
        groups.push({ subject, subjectIndex, volume, volumeIndex, matchedUnits });
      }
    });
  });
  return groups;
}

function renderSearch() {
  const groups = getSearchResults();
  volumeTabs.hidden = true;
  breadcrumb.innerHTML = `
    <div>
      <h2>搜索结果</h2>
      <p>关键词「${escapeHTML(state.query)}」共匹配 ${groups.reduce((sum, group) => sum + group.matchedUnits.length, 0)} 项</p>
    </div>
  `;

  if (!groups.length) {
    unitList.innerHTML = `
      <div class="empty">
        <div>
          <strong>没有找到匹配内容</strong>
          <p>可以换一个关键词，例如“函数”“红楼梦”“生态”“Unit 1”。</p>
        </div>
      </div>
    `;
    return;
  }

  unitList.innerHTML = groups
    .map(
      ({ subject, subjectIndex, volumeIndex, matchedUnits }) => `
        ${
          state.mode === "mindmap"
            ? `
              <article class="volume-card">
                <div class="volume-card__head">
                  <h3>${highlight(subject.name)} · ${highlight(subject.volumes[volumeIndex].name)}</h3>
                  <small>${matchedUnits.length} 张导图</small>
                </div>
              </article>
              ${mindMapListHTML(subjectIndex, volumeIndex, matchedUnits)}
            `
            : `
              <article class="volume-card">
                <div class="volume-card__head">
                  <h3>${highlight(subject.name)} · ${highlight(subject.volumes[volumeIndex].name)}</h3>
                  <small>${matchedUnits.length} 项</small>
                </div>
                <div class="units">
                  ${matchedUnits
                    .map(({ unit, unitIndex }) =>
                      unitHTML(subjectIndex, volumeIndex, unit, unitIndex, subject.volumes[volumeIndex].name),
                    )
                    .join("")}
                </div>
              </article>
            `
        }
      `,
    )
    .join("");
}

function renderSubjectMapSearch() {
  const groups = getSearchResults();
  const subjectIndexes = [...new Set(groups.map((group) => group.subjectIndex))];
  volumeTabs.hidden = true;
  breadcrumb.innerHTML = `
    <div>
      <h2>跨册总图搜索</h2>
      <p>关键词「${escapeHTML(state.query)}」命中 ${subjectIndexes.length} 个学科，将展示对应学科的全册知识关系网。</p>
    </div>
  `;

  if (!subjectIndexes.length) {
    unitList.innerHTML = `
      <div class="empty">
        <div>
          <strong>没有找到匹配学科总图</strong>
          <p>可以换一个关键词，例如“函数”“电磁感应”“生态”“法治”。</p>
        </div>
      </div>
    `;
    return;
  }

  unitList.innerHTML = subjectIndexes.map((subjectIndex) => subjectNetworkHTML(subjectIndex)).join("");
}

function render() {
  renderStats();
  renderModeSwitch();
  renderSubjects();
  if (state.mode === "methods") {
    renderLearningMethods();
  } else if (state.mode === "wordTrainer") {
    renderWordTrainer();
  } else if (state.mode === "videos") {
    renderBilibiliVideos();
  } else if (state.query.trim() && state.mode === "subjectMap") {
    renderSubjectMapSearch();
  } else if (state.query.trim()) {
    renderSearch();
  } else {
    renderCurrentVolume();
  }
}

subjectList.addEventListener("click", (event) => {
  const button = event.target.closest("[data-subject-index]");
  if (!button) return;
  state.subjectIndex = Number(button.dataset.subjectIndex);
  state.volumeIndex = 0;
  if (["methods", "wordTrainer"].includes(state.mode)) state.mode = "catalog";
  state.query = "";
  searchInput.value = "";
  render();
});

volumeTabs.addEventListener("click", (event) => {
  const button = event.target.closest("[data-volume-index]");
  if (!button) return;
  state.volumeIndex = Number(button.dataset.volumeIndex);
  render();
});

modeSwitch.addEventListener("click", (event) => {
  const button = event.target.closest("[data-mode]");
  if (!button) return;
  state.mode = button.dataset.mode;
  render();
});

unitList.addEventListener("click", (event) => {
  if (handleWordTrainerClick(event)) return;
  const button = event.target.closest("[data-go-unit]");
  if (!button) return;
  const [subjectIndex, volumeIndex, unitIndex] = button.dataset.goUnit.split("-").map(Number);
  state.subjectIndex = subjectIndex;
  state.volumeIndex = volumeIndex;
  state.mode = "mindmap";
  state.query = "";
  searchInput.value = "";
  render();
  const target = document.querySelector(".content-panel");
  if (target) target.scrollIntoView({ behavior: "smooth", block: "start" });
});

unitList.addEventListener("change", (event) => {
  const checkbox = event.target.closest("[data-progress-key]");
  if (!checkbox) return;
  if (checkbox.checked) {
    doneSet.add(checkbox.dataset.progressKey);
  } else {
    doneSet.delete(checkbox.dataset.progressKey);
  }
  saveProgress();
  render();
});

searchInput.addEventListener("input", (event) => {
  state.query = event.target.value;
  render();
});

resetBtn.addEventListener("click", () => {
  state.query = "";
  searchInput.value = "";
  render();
  searchInput.focus();
});

try {
  render();
} finally {
  hidePageLoader();
}
